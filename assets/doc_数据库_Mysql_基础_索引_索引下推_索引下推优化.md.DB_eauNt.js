import{_ as i,c as t,m as e,a,t as n,a5 as o,o as s}from"./chunks/framework.DwbewbAn.js";const l="/assets/008i3skNgy1gtxqek3rlrj614w0jadj902.N9fyluvI.jpg",p="/assets/008i3skNgy1gtxp1xkmr1j61gu09idgv02.CkY5C35-.jpg",d="/assets/008i3skNgy1gtxpnkjr0uj640v0r84kt02.Bi76k9BV.jpg",c="/assets/008i3skNgy1gtxqesorw6j60tw0emmz102.DR3E7bqr.jpg",g="/assets/008i3skNgy1gtxq7l135mj61iw08iq4102.LD2Kilmq.jpg",u=JSON.parse('{"title":"索引下推","description":"","frontmatter":{"title":"索引下推"},"headers":[],"relativePath":"doc/数据库/Mysql/基础/索引/索引下推/索引下推优化.md","filePath":"packages/doc/src/数据库/Mysql/基础/索引/索引下推/索引下推优化.md","lastUpdated":1711637456000}'),h={name:"doc/数据库/Mysql/基础/索引/索引下推/索引下推优化.md"},I={id:"frontmatter-title",tabindex:"-1"},_=e("a",{class:"header-anchor",href:"#frontmatter-title","aria-label":'Permalink to "{{ $frontmatter.title }}"'},"​",-1),m=o('<p><strong>背景</strong></p><p>索引下推(Index Condition Pushdown) ICP 是Mysql5.6之后新增的功能，主要的核心点就在于把数据筛选的过程放在了存储引擎层去处理，而不是像之前一样放到Server层去做过滤。</p><p>虽然这是一个比较简单的概念，但是可能很多不细心的同学对于索引下推会存在一个小小的误区，至于是什么，请看下文。</p><h3 id="什么是索引下推" tabindex="-1">什么是索引下推 <a class="header-anchor" href="#什么是索引下推" aria-label="Permalink to &quot;什么是索引下推&quot;">​</a></h3><p>首先，我们创建一张<code>user</code>表，同时建立<code>age_name</code>的联合索引，同时插入3条测试数据。</p><p><img src="'+l+'" alt="img"></p><p>然后，我们执行查询<code>explain SELECT * from user where age &gt;10 and name = &#39;a&#39;</code>，如下图所示，就会看见<code>Extra</code>中显示了<code>Using index condition</code>，你可能就知道了，这表示出现了<strong>索引下推</strong>了。</p><p><img src="'+p+'" alt="img"></p><p>没错，针对这个查询场景就是索引下推，那到底什么是索引下推呢？</p><p>按照我们上述的场景，实际上就存在两个索引树，一个是主键索引，存储了具体的数据的信息，另外则是<code>age_name</code>的联合索引，保存了主键的ID。</p><p><img src="'+d+'" alt="img"></p><p>在没有ICP索引下推的时候，这个查询的流程应该是这样（略过无关的细节）：</p><ol><li>Mysql Server层调用API查询存储引擎数据</li><li>存储引擎根据联合索引首先通过条件找到所有age&gt;10的数据</li><li>找到的每一条数据都根据主键索引进行回表查询，直到找到不符合条件的结果</li><li>返回数据给Server层，Server根据条件对结果进行过滤，流程结束</li></ol><p>而有了ICP之后的流程则是这样：</p><ol><li>Mysql Server层调用API查询存储引擎数据</li><li>存储引擎根据联合索引首先通过条件找到所有age&gt;10的数据，根据联合索引中已经存在的<code>name</code>数据进行过滤，找到符合条件的数据</li><li>根据找到符合条件的数据，回表查询</li><li>返回数据给Server层，流程结束</li></ol><p>对比这两个流程就会很明显的发现，使用ICP之后我们就是简单的通过联合索引中本来就有的数据直接过滤了，不需要再查到一堆无用的数据去Server层进行过滤，这样的话减少了回表的次数和返回的数据，IO次数减少了，对性能有很好的提升。</p><p>按照官方文档所说，ICP其实也存在一定的使用限制场景，只说关键的，乱七八糟的不说。</p><ol><li>首先，ICP适用于range、ref、eq_ref和ref_or_null的场景下</li><li>InnoDB和MyISAM都支持ICP，Mysql partition分表的话也可以使用</li><li>对于InndoDB而言，ICP只支持二级索引，因为主键索引它用不上不是吗？</li><li>子查询不支持</li></ol><p>现在我们基本都使用的5.6以上的版本了，默认就是开启ICP的，想关闭的话可以通过命令<code>SET optimizer_switch = &#39;index_condition_pushdown=off&#39;;</code>。</p><h3 id="一个小小的误区" tabindex="-1">一个小小的误区 <a class="header-anchor" href="#一个小小的误区" aria-label="Permalink to &quot;一个小小的误区&quot;">​</a></h3><p>一般来说，正常情况下Mysql一次查询都只能走一个索引，我们来修改上述的表结构，把联合索引改为两个单独的索引，数据保持不变</p><p><img src="'+c+'" alt="img"></p><p>然后我们执行查询<code>explain SELECT * from user where age &gt;10 and name like &#39;a%&#39;</code>，结果如下图。</p><p><img src="'+g+'" alt="img"></p><p>你会发现，我靠，怎么还有索引下推？这不科学对不对，好像无法解释嘛，难道这一次索引下推还能先查出<code>age</code>再下推到<code>name</code>索引吗，这完全不合理啊。</p><h2 id="其实不然-真实的情况是-using-index-condition并不代表一定是使用了索引下推-只是代表可以使用-但是不一定用了。。。" tabindex="-1">其实不然，真实的情况是，<code>Using index condition</code>并不代表一定是使用了索引下推，只是代表可以使用，但是不一定用了。。。 <a class="header-anchor" href="#其实不然-真实的情况是-using-index-condition并不代表一定是使用了索引下推-只是代表可以使用-但是不一定用了。。。" aria-label="Permalink to &quot;其实不然，真实的情况是，`Using index condition`并不代表一定是使用了索引下推，只是代表可以使用，但是不一定用了。。。&quot;">​</a></h2><h2 id="我们说索引下推一定是在联合索引的情况下-根据联合索引本身就有的数据直接做一次过滤-而不用再进行多次无用的回表再到server层进行过滤-这一点你要很明确才行。" tabindex="-1">我们说索引下推一定是在联合索引的情况下，根据联合索引本身就有的数据直接做一次过滤，而不用再进行多次无用的回表再到Server层进行过滤，这一点你要很明确才行。 <a class="header-anchor" href="#我们说索引下推一定是在联合索引的情况下-根据联合索引本身就有的数据直接做一次过滤-而不用再进行多次无用的回表再到server层进行过滤-这一点你要很明确才行。" aria-label="Permalink to &quot;我们说索引下推一定是在联合索引的情况下，根据联合索引本身就有的数据直接做一次过滤，而不用再进行多次无用的回表再到Server层进行过滤，这一点你要很明确才行。&quot;">​</a></h2><h2 id="一、sql的where条件提取规则" tabindex="-1">一、SQL的where条件提取规则 <a class="header-anchor" href="#一、sql的where条件提取规则" aria-label="Permalink to &quot;一、SQL的where条件提取规则&quot;">​</a></h2><p>在ICP（Index Condition Pushdown，索引条件下推）特性之前，必须先搞明白根据何登成大神总结出一套放置于所有SQL语句而皆准的where查询条件的提取规则：所有SQL的where条件，均可归纳为3大类：Index Key (First Key &amp; Last Key)，Index Filter，Table Filter。</p><p>接下来，简单说一下这3大类分别是如何定义，以及如何提取的，详情请看：<a href="http://hedengcheng.com/?p=577" target="_blank" rel="noreferrer">SQL语句中where条件在数据库中提取与应用浅析</a>。</p><p><strong>Index Key(Fist key &amp; Last Key)，Index Filter，Table Filter</strong></p><p><strong>Index First Key</strong></p><p>只是用来定位索引的起始范围，因此只在索引第一次Search Path(沿着索引B+树的根节点一直遍历，到索引正确的叶节点位置)时使用，一次判断即可；</p><p><strong>Index Last Key</strong></p><p>用来定位索引的终止范围，因此对于起始范围之后读到的每一条索引记录，均需要判断是否已经超过了Index Last Key的范围，若超过，则当前查询结束；</p><p><strong>Index Filter</strong></p><p>用于过滤索引查询范围中不满足查询条件的记录，因此对于索引范围中的每一条记录，均需要与Index Filter进行对比，若不满足Index Filter则直接丢弃，继续读取索引下一条记录；</p><p><strong>Table Filter</strong></p><p>则是最后一道where条件的防线，用于过滤通过前面索引的层层考验的记录，此时的记录已经满足了Index First Key与Index Last Key构成的范围，并且满足Index Filter的条件，回表读取了完整的记录，判断完整记录是否满足Table Filter中的查询条件，同样的，若不满足，跳过当前记录，继续读取索引的下一条记录，若满足，则返回记录，此记录满足了where的所有条件，可以返回给前端用户。</p><h2 id="二、icp特性介绍" tabindex="-1">二、ICP特性介绍 <a class="header-anchor" href="#二、icp特性介绍" aria-label="Permalink to &quot;二、ICP特性介绍&quot;">​</a></h2><p>Index Condition Pushdown (ICP)是MySQL 5.6版本中的新特性，是一种在存储引擎层使用索引过滤数据的一种优化方式。</p><p>我对Using index condition的理解是，首先mysql server和storage engine是两个组件，server负责sql的parse，执行； storage engine去真正的做数据/index的读取/写入。以前是这样：server命令storage engine按index key把相应的数据从数据表读出，传给server，然后server来按where条件（index filter和table filter）做选择。</p><p>而在MySQL 5.6加入ICP后，Index Filter与Table Filter分离，Index Filter下降到InnoDB的索引层面进行过滤，如果不符合条件则无须读数据表，减少了回表与返回MySQL Server层的记录交互开销，节省了disk IO，提高了SQL的执行效率。</p><p><strong>原理</strong></p><p>a. 当关闭ICP时，index仅仅是data access的一种访问方式，存储引擎通过索引回表获取的数据会传递到MySQL Server层进行where条件过滤，也就是做index filter和table filter。</p><p>b. 当打开ICP时，如果部分where条件能使用索引中的字段，MySQL Server会把这部分下推到引擎层，可以利用index filter的where条件在存储引擎层进行数据过滤，而非将所有通过index access的结果传递到MySQL server层进行where过滤。</p><p>优化效果：ICP能减少引擎层访问基表的次数和MySQL Server访问存储引擎的次数，减少io次数，提高查询语句性能。</p><p>开启ICP时，MySQL server把index filter（last_name like ‘%nta’）推到存储引擎层，在存储引擎内部通过与where条件last_name like ‘%nta’的对比，直接过滤掉不符合条件的数据，然后返回最终数据给MySQL server层。该过程减少了回表操作，只访问符合条件的1条记录并返回给MySQL Server ，有效的减少了io访问和各层之间的交互。</p><p>ICP关闭时 ，仅仅使用索引作为访问数据的方式。</p><p>ICP 开启时 ，MySQL将在存储引擎层 利用索引过滤数据，减少不必要的回表。</p><p>注意虚线的using where表示如果where条件中含有没有被索引的字段，则还是要经过MySQL Server 层过滤。</p><ol><li><p>当sql需要全表访问时，ICP的优化策略可用于range, ref, eq_ref, ref_or_null类型的访问数据方法 。</p></li><li><p>支持InnoDB和MyISAM表。</p></li><li><p>ICP只能用于二级索引，不能用于主索引。</p></li><li><p>并非全部where条件都可以用ICP筛选，如果where条件的字段不在索引列中，还是要读取整表的记录到server端做where过滤。</p></li><li><p>ICP的加速效果取决于在存储引擎内通过ICP筛选掉的数据的比例。</p></li><li><p>MySQL 5.6版本的不支持分表的ICP功能，5.7版本的开始支持。</p></li><li><p>当sql使用覆盖索引时，不支持ICP优化方法。</p></li></ol>',52);function x(r,S,P,y,f,C){return s(),t("div",null,[e("h1",I,[a(n(r.$frontmatter.title)+" ",1),_]),m])}const v=i(h,[["render",x]]);export{u as __pageData,v as default};
