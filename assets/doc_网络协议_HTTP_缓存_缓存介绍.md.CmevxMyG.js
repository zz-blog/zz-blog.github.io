import{_ as i,c as l,m as a,a as t,t as s,a5 as o,o as p}from"./chunks/framework.DwbewbAn.js";const r="/assets/img-%E7%BC%93%E5%AD%98%E6%B5%81%E7%A8%8B.Dl6QlHzM.png",n="/assets/2021082011371614.CDpOypNu.png",E=JSON.parse('{"title":"HTTP缓存详解","description":"","frontmatter":{"title":"HTTP缓存详解"},"headers":[],"relativePath":"doc/网络协议/HTTP/缓存/缓存介绍.md","filePath":"packages/doc/src/网络协议/HTTP/缓存/缓存介绍.md","lastUpdated":1704643216000}'),c={name:"doc/网络协议/HTTP/缓存/缓存介绍.md"},d={id:"frontmatter-title",tabindex:"-1"},h=a("a",{class:"header-anchor",href:"#frontmatter-title","aria-label":'Permalink to "{{ $frontmatter.title }}"'},"​",-1),u=o('<ul><li><a href="https://segmentfault.com/a/1190000010690320" target="_blank" rel="noreferrer">参考</a></li><li><a href="https://zhuanlan.zhihu.com/p/29750583" target="_blank" rel="noreferrer">参考</a></li><li><a href="https://juejin.cn/post/6844903634002509832" target="_blank" rel="noreferrer">参考</a></li></ul><h2 id="什么是浏览器缓存" tabindex="-1">什么是浏览器缓存 <a class="header-anchor" href="#什么是浏览器缓存" aria-label="Permalink to &quot;什么是浏览器缓存&quot;">​</a></h2><p>Web缓存是指一个Web资源（如html页面，图片，js，数据等）存在于Web服务器和客户端（浏览器）之间的副本。缓存会根据进来的请求保存输出内容的副本；当下一个请求来到的时候，如果是相同的URL，缓存会根据缓存机制决定是直接使用副本响应访问请求，还是向源服务器再次发送请求。</p><p>比较常见的就是浏览器会缓存访问过网站的网页，当再次访问这个URL地址的时候，如果网页没有更新，就不会再次下载网页，而是直接使用本地缓存的网页。</p><p>只有当网站明确标识资源已经更新，浏览器才会再次下载网页。浏览器和网站服务器是根据缓存机制进行缓存的</p><h2 id="非http协议定义的缓存机制" tabindex="-1">非HTTP协议定义的缓存机制 <a class="header-anchor" href="#非http协议定义的缓存机制" aria-label="Permalink to &quot;非HTTP协议定义的缓存机制&quot;">​</a></h2><p>浏览器缓存机制，其实主要就是HTTP协议定义的缓存机制（如： Expires； Cache-control等）。但是也有非HTTP协议定义的缓存机制，如使用HTML Meta 标签，Web开发者可以在HTML页面的节点中加入标签</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>&lt;``meta` `http-equiv``=``&quot;Pragma&quot;` `content``=``&quot;no-cache&quot;``&gt;</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>上述代码的作用是告诉浏览器当前页面不被缓存，每次访问都需要去服务器拉取。使用上很简单，但只有部分浏览器可以支持，而且所有缓存代理服务器都不支持，因为代理不解析HTML内容本身。</p><h2 id="缓存流程图" tabindex="-1">缓存流程图 <a class="header-anchor" href="#缓存流程图" aria-label="Permalink to &quot;缓存流程图&quot;">​</a></h2><p>利用浏览器缓存的过程：</p><p><img src="'+r+'" alt="在这里插入图片描述"></p><h2 id="http缓存机制" tabindex="-1">HTTP缓存机制 <a class="header-anchor" href="#http缓存机制" aria-label="Permalink to &quot;HTTP缓存机制&quot;">​</a></h2><p>根据Response Header里面的Cache-Control和Expires这两个属性，当两个都存在时，Cache-Control优先级较高。</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>Cache-Control</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p><img src="'+n+`" alt="在这里插入图片描述"></p><p>该字段用于控制浏览器在什么情况下直接使用本地缓存而不向服务器发送请求。一般具有以下值：</p><ul><li><code>Public</code>：指示响应可被任何缓存区缓存。</li><li><code>Private</code>：指示对于单个用户的整个或部分响应消息，不能被共享缓存处理。这允许服务器仅仅描述当用户的部分响应消息，此响应消息对于其他用户的请求无效。</li><li><code>no-cache</code>：指示请求或响应消息不能缓存。</li><li><code>no-store</code>：用于防止重要的信息被无意的发布。在请求消息中发送将使得请求和响应消息都不使用缓存。</li><li><code>max-age</code>：指示浏览器可以接收生存期不大于指定时间（<strong>以秒为单位</strong>）的响应。</li><li><code>min-fresh</code>：指示浏览器可以接收响应时间小于当前时间加上指定时间的响应。</li><li><code>max-stale</code>：指示浏览器可以接收超出超时期间的响应消息。如果指定max-stale消息的值，那么浏览器可以接收超出超时期指定值之内的响应消息。</li></ul><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>Expires（石器时代的缓存机制）</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Expires 头部字段提供一个日期和时间，在该日期前的所有对该资源的请求都会直接使用浏览器缓存而不用向服务器请求。</p><p>例如：Expires: Sun, 08 Nov 2009 03:37:26 GMT</p><p>注意：</p><ul><li>cache-control max-age 和 max-stale将覆盖Expires header。</li><li>使用Expires存在服务器端时间和浏览器时间不一致的问题。</li><li>另外有人说Expires 是HTTP 1.0的东西，现在默认浏览器均默认使用HTTP 1.1。</li></ul><h2 id="服务端如何判断缓存已失效" tabindex="-1">服务端如何判断缓存已失效 <a class="header-anchor" href="#服务端如何判断缓存已失效" aria-label="Permalink to &quot;服务端如何判断缓存已失效&quot;">​</a></h2><p>服务端通过If-Modified-Since（Last-Modified）和If-None-Match（Etag）这两个属性的值来判断缓存是否失效的。</p><h3 id="last-modified-if-modified-since" tabindex="-1">Last-Modified/If-Modified-Since <a class="header-anchor" href="#last-modified-if-modified-since" aria-label="Permalink to &quot;Last-Modified/If-Modified-Since&quot;">​</a></h3><p>Last-Modified/If-Modified-Since要配合Cache-Control使用。</p><p><code>Last-Modified</code>：响应资源的最后修改时间。</p><p><code>If-Modified-Since</code>：当缓存过期时，发现资源具有Last-Modified声明，则在请求头带上If-Modified-Since（值就是Last-Modified）。服务器收到请求后发现有头If-Modified-Since则与被请求资源的最后修改时间进行比对。若最后修改时间较新，说明资源又被改动过，则响应HTTP 200整片资源内容（写在响应消息包体内）;若最后修改时间较旧，说明资源无新修改，则响应HTTP 304，告知浏览器继续使用所保存的cache。</p><h3 id="etag-if-none-match" tabindex="-1">Etag/If-None-Match <a class="header-anchor" href="#etag-if-none-match" aria-label="Permalink to &quot;Etag/If-None-Match&quot;">​</a></h3><p>Etag/If-None-Match也要配合Cache-Control使用。</p><p><code>Etag</code>：资源在服务器的唯一标识（生成规则由服务器决定）。Apache中，ETag的值，默认是对文件的索引节（INode），大小（Size）和最后修改时间（MTime）进行Hash后得到的。</p><p><code>If-None-Match</code>：当缓存过期时，发现资源具有Etage声明，则在请求头带上If-None-Match（值就是Etag）。服务器收到请求后发现有头If-None-Match 则与被请求资源的相应校验串进行比对，决定返回200或304。</p><h2 id="为什么有了last-modified还要etag" tabindex="-1">为什么有了Last-Modified还要Etag? <a class="header-anchor" href="#为什么有了last-modified还要etag" aria-label="Permalink to &quot;为什么有了Last-Modified还要Etag?&quot;">​</a></h2><p>Etag的出现主要是为了解决几个Last-Modified比较难解决的问题：</p><ul><li>Last-Modified标注的最后修改只能精确到秒级，如果某些文件在1秒钟以内，被修改多次的话，它将不能准确标注文件的修改时间。</li><li>如果某些文件会被定期生成，当有时内容并没有任何变化，但Last-Modified却改变了，导致文件没法使用缓存。</li><li>有可能存在服务器没有准确获取文件修改时间，或者与代理服务器时间不一致等情形。</li></ul><h2 id="_200-ok-from-cache-与304-not-modified的区别" tabindex="-1">200 OK（from cache）与304 Not Modified的区别 <a class="header-anchor" href="#_200-ok-from-cache-与304-not-modified的区别" aria-label="Permalink to &quot;200 OK（from cache）与304 Not Modified的区别&quot;">​</a></h2><p>200 OK( from cache )不向服务器发送请求，直接使用本地缓存文件。304 Not Modified则向服务器询问，若服务器认为浏览器的缓存版本还可用，那么便会返回304。</p><h3 id="_200-ok-from-cache-出现操作" tabindex="-1">200 OK( from cache ) 出现操作： <a class="header-anchor" href="#_200-ok-from-cache-出现操作" aria-label="Permalink to &quot;200 OK( from cache ) 出现操作：&quot;">​</a></h3><p>1.地址栏回车</p><p>2.页面链接跳转</p><p>3.前进、后退</p><h3 id="_304-not-modified-出现操作" tabindex="-1">304 Not Modified 出现操作： <a class="header-anchor" href="#_304-not-modified-出现操作" aria-label="Permalink to &quot;304 Not Modified 出现操作：&quot;">​</a></h3><p>1.F5刷新</p><p>2.新开窗口</p><h2 id="缓存的不同来源" tabindex="-1">缓存的不同来源 <a class="header-anchor" href="#缓存的不同来源" aria-label="Permalink to &quot;缓存的不同来源&quot;">​</a></h2><p><code>from disk cache</code>：从磁盘中获取缓存资源，等待下次访问时不需要重新下载资源，而直接从磁盘中获取。它的直接操作对象为CurlCacheManager。</p><p><code>from memory cache</code>：从内存中获取资源，等待下次访问时不需要重新下载资源，而直接从内存中获取。</p><p>**两者区别：**当退出进程时，内存中的数据会被清空，而磁盘的数据不会，所以，当下次再进入该进程时，该进程仍可以从diskCache中获得数据，而memoryCache则不行。</p><h2 id="不能被缓存的请求" tabindex="-1">不能被缓存的请求 <a class="header-anchor" href="#不能被缓存的请求" aria-label="Permalink to &quot;不能被缓存的请求&quot;">​</a></h2><p>当然并不是所有请求都能被缓存。</p><p>无法被浏览器缓存的请求：</p><ul><li>HTTP信息头中包含Cache-Control:no-cache，pragma:no-cache（HTTP1.0），或Cache-Control:max-age=0等告诉浏览器不用缓存的请求</li><li>需要根据Cookie，认证信息等决定输入内容的动态请求是不能被缓存的</li><li>经过HTTPS安全加密的请求</li><li>POST请求无法被缓存</li><li>HTTP响应头中不包含Last-Modified/Etag，也不包含Cache-Control/Expires的请求无法被缓存</li></ul><p>#############################################################################################</p><h1 id="面试精选之http缓存" tabindex="-1">面试精选之http缓存 <a class="header-anchor" href="#面试精选之http缓存" aria-label="Permalink to &quot;面试精选之http缓存&quot;">​</a></h1><p>前端面试常问第二大问题是<code>http缓存</code>相关内容。说真的，http缓存相关的细节比较多，并且 http 常用协议版本有1.0、1.1，（本文暂不讨论http2.0）。</p><h3 id="缓存相关-header" tabindex="-1">缓存相关 header <a class="header-anchor" href="#缓存相关-header" aria-label="Permalink to &quot;缓存相关 header&quot;">​</a></h3><p>我们先罗列一下和缓存相关的请求响应头。</p><ul><li>Expires</li></ul><blockquote><p>响应头，代表该资源的过期时间。</p></blockquote><ul><li>Cache-Control</li></ul><blockquote><p>请求/响应头，缓存控制字段，精确控制缓存策略。</p></blockquote><ul><li>If-Modified-Since</li></ul><blockquote><p>请求头，资源最近修改时间，由浏览器告诉服务器。</p></blockquote><ul><li>Last-Modified</li></ul><blockquote><p>响应头，资源最近修改时间，由服务器告诉浏览器。</p></blockquote><ul><li>Etag</li></ul><blockquote><p>响应头，资源标识，由服务器告诉浏览器。</p></blockquote><ul><li>If-None-Match</li></ul><blockquote><p>请求头，缓存资源标识，由浏览器告诉服务器。</p></blockquote><p>配对使用的字段：</p><ul><li>If-Modified-Since 和 Last-Modified</li><li>Etag 和 If-None-Match</li></ul><p>今天着重介绍一下浏览器缓存机制，我们知道，浏览器缓存一般都是针对静态资源，比如 js、css、图片 等，所以我们下面的例子围绕一个 javascript 文件 a.js 来阐述。抛开理论式灌输，我们从实际场景触发，一点点完善缓存机制，这种方式，相信大家会更容易理解。</p><p>做一些约定，方便以后比较。</p><ul><li>a.js 大小为 10 KB</li><li>请求头约定为 1 KB</li><li>响应头约定为 1 KB</li></ul><h3 id="原始模型" tabindex="-1">原始模型 <a class="header-anchor" href="#原始模型" aria-label="Permalink to &quot;原始模型&quot;">​</a></h3><ul><li>浏览器请求静态资源 a.js。（请求头：1KB）</li><li>服务器读取磁盘文件 a.js，返给浏览器。（10KB（a.js）+1KB（响应头） = 11KB）。</li><li>浏览器再次请求，服务器又重新读取磁盘文件 a.js，返给浏览器。</li><li>如此循环。。</li></ul><p>执行一个往返，流量为 10（a.js）+1（请求头）+1（响应头） = 12KB。</p><p>访问 10 次，流量大约为12 * 10 = 120KB。</p><p>所以，流量与访问次数有关：</p><p>L（流量） = N（访问次数） * 12。</p><p>该方式缺点很明显：</p><ul><li>浪费用户流量。</li><li>浪费服务器资源，服务器要读磁盘文件，然后发送文件到浏览器。</li><li>浏览器要等待 a.js 下载并且执行后才能渲染页面，影响用户体验。</li></ul><blockquote><p>js 执行时间相比下载时间要快的多，如果能优化下载时间，用户体验会提升很多。</p></blockquote><h3 id="浏览器增加缓存机制" tabindex="-1">浏览器增加缓存机制 <a class="header-anchor" href="#浏览器增加缓存机制" aria-label="Permalink to &quot;浏览器增加缓存机制&quot;">​</a></h3><ul><li>浏览器第一次请求 a.js，缓存 a.js 到本地磁盘。（1+10+1 =12KB）</li><li>浏览器再次请求 a.js，直接走浏览器缓存（200，from cache），不再向服务器发起请求。（0KB）</li><li>...</li></ul><p>第一次访问，流量为 1+10+1 = 12KB。 第二次访问，流量为 0。 。。。 第 10000 次访问，流量依然为 0。</p><p>所以流量与访问次数无关：</p><p>L（流量） = 12KB。</p><p>优点：</p><ul><li>大大减少带宽。</li><li>由于减少了 a.js 下载时间，相应的提高了用户体验。</li></ul><p>缺点：服务器上 a.js 更新时，浏览器感知不到，拿不到最新的 js 资源。</p><h3 id="服务器和浏览器约定资源过期时间。" tabindex="-1">服务器和浏览器约定资源过期时间。 <a class="header-anchor" href="#服务器和浏览器约定资源过期时间。" aria-label="Permalink to &quot;服务器和浏览器约定资源过期时间。&quot;">​</a></h3><p>服务器和浏览器约定文件过期时间，用 Expires 字段来控制，时间是 GMT 格式的标准时间，如 Fri, 01 Jan 1990 00:00:00 GMT。</p><ul><li>浏览器第一次请求一个静态资源 a.js。（1KB）</li><li>服务器把 a.js 和 a.js 的缓存过期时间(Expires：Mon, 26 Sep 2018 05:00:00 GMT)发给浏览器。（10+1=11KB）</li></ul><blockquote><p>服务器告诉浏览器：你把我发给你的 a.js 文件缓存到你那里，在 2018年9月26日5点之前不要再发请求烦我，直接使用你自己缓存的 a.js 就行了。</p></blockquote><ul><li>浏览器接收到 a.js，同时记住了过期时间。</li><li>在2018年9月26日5点之前，浏览器再次请求 a.js，便不再请求服务器，直接使用上一次缓存的 a.js 文件。（0KB）</li><li>在2018年9月26日5点01分，浏览器请求 a.js，发现 a.js 缓存时间过了，于是不再使用本地缓存，而是请求服务器，服务器又重新读取磁盘文件 a.js，返给浏览器，同时告诉浏览器一个新的过期时间。（1+10+1=12KB）。</li><li>如此往复。。。</li></ul><p>该种方式较之前的方式有了很大的改善：</p><ul><li>在过期时间以内，为用户省了很多流量。</li><li>减少了服务器重复读取磁盘文件的压力。</li><li>缓存过期后，能够得到最新的 a.js 文件。</li></ul><p>缺点还是有：</p><ul><li>缓存过期以后，服务器不管 a.js有没有变化，都会再次读取 a.js文件，并返给浏览器。</li></ul><h3 id="服务器告诉浏览器资源上次修改时间。" tabindex="-1">服务器告诉浏览器资源上次修改时间。 <a class="header-anchor" href="#服务器告诉浏览器资源上次修改时间。" aria-label="Permalink to &quot;服务器告诉浏览器资源上次修改时间。&quot;">​</a></h3><p>为了解决上个方案的问题，服务器和浏览器经过磋商，制定了一种方案，服务器每次返回 a.js 的时候，还要告诉浏览器 a.js 在服务器上的最近修改时间 Last-Modified （GMT标准格式）。</p><ul><li>浏览器访问 a.js 文件。（1KB）</li><li>服务器返回 a.js 的时候，告诉浏览器 a.js 文件。（10+1=11KB） 在服务器的上次修改时间 Last-Modified（GMT标准格式）以及缓存过期时间 Expires（GMT标准格式）</li><li>当 a.js 过期时，浏览器带上 If-Modified-Since（等于上一次请求的Last-Modified） 请求服务器。（1KB）</li><li>服务器比较请求头里的 Last-Modified 时间和服务器上 a.js的上次修改时间： <ul><li>如果一致，则告诉浏览器：你可以继续用本地缓存（304）。此时，服务器不再返回 a.js 文件。（1KB）</li><li>如果不一致，服务器读取磁盘上的 a.js 文件返给浏览器，同时告诉浏览器 a.js 的最近的修改时间 Last-Modified 以及过期时间 Expires。（1+10=11KB）</li><li>如此往复。</li></ul></li></ul><p>此种方案比上一个方案有了更进一步的优化：</p><ul><li>缓存过期后，服务器检测如果文件没变化，不再把a.js发给浏览器，省去了 10KB 的流量。</li><li>缓存过期后，服务器检测文件有变化，则把最新的 a.js 发给浏览器，浏览器能够得到最新的 a.js。</li></ul><p>缺点：</p><ul><li>Expires 过期控制不稳定，因为浏览器端可以随意修改时间，导致缓存使用不精准。</li><li>Last-Modified 过期时间只能精确到秒。</li></ul><p>精确到秒存在两个问题：</p><ul><li>1、如果 a.js 在一秒时间内经常变动，同时服务器给 a.js 设置无缓存，那浏览器每次访问 a.js，都会请求服务器，此时服务器比较发给浏览器的上次修改时间和 a.js 的最近修改时间，发现都是在同一时间（因为精确到秒），因此返回给浏览器继续使用本地缓存的消息（304），但事实上服务器上的 a.js 已经改动了好多次了。所以这种情况，浏览器拿不到最新的 a.js 文件。</li><li>2、如果在服务器上 a.js 被修改了，但其实际内容根本没发生改变，会因为 Last-Modified 时间匹配不上而重新返回 a.js 给浏览器。</li></ul><h3 id="继续改进-增加相对时间的控制-引入-cache-contorl" tabindex="-1">继续改进，增加相对时间的控制，引入 Cache-Contorl <a class="header-anchor" href="#继续改进-增加相对时间的控制-引入-cache-contorl" aria-label="Permalink to &quot;继续改进，增加相对时间的控制，引入 Cache-Contorl&quot;">​</a></h3><p>为了兼容已经实现了上述方案的浏览器，同时加入新的缓存方案，服务器除了告诉浏览器 Expires ，同时告诉浏览器一个相对时间 Cache-Control：max-age=10秒。意思是在10秒以内，使用缓存到浏览器的 a.js 资源。</p><p>浏览器先检查 Cache-Control，如果有，则以 Cache-Control 为准，忽略 Expires。如果没有 Cache-Control，则以 Expires 为准。</p><h3 id="继续改进-增加文件内容对比-引入etag" tabindex="-1">继续改进，增加文件内容对比，引入Etag <a class="header-anchor" href="#继续改进-增加文件内容对比-引入etag" aria-label="Permalink to &quot;继续改进，增加文件内容对比，引入Etag&quot;">​</a></h3><p>为了解决文件修改时间只能精确到秒带来的问题，我们给服务器引入 Etag 响应头，a.js 内容变了，Etag 才变。内容不变，Etag 不变，可以理解为 Etag 是文件内容的唯一 ID。 同时引入对应的请求头 If-None-Match，每次浏览器请求服务器的时候，都带上If-None-Match字段，该字段的值就是上次请求 a.js 时，服务器返回给浏览器的 Etag。</p><ul><li>浏览器请求 a.js。</li><li>服务器返回 a.js，同时告诉浏览器过期绝对时间（Expires）以及相对时间（Cache-Control：max-age=10），以及a.js上次修改时间Last-Modified，以及 a.js 的Etag。</li><li>10秒内浏览器再次请求 a.js，不再请求服务器，直接使用本地缓存。</li><li>11秒时，浏览器再次请求 a.js，请求服务器，带上上次修改时间 If-Modified-Since 和上次的 Etag 值 If-None-Match。</li><li>服务器收到浏览器的If-Modified-Since和Etag，发现有If-None-Match，则比较 If-None-Match 和 a.js 的 Etag 值，忽略If-Modified-Since的比较。</li><li>a.js 文件内容没变化，则Etag和If-None-Match 一致，服务器告诉浏览器继续使用本地缓存（304）。</li><li>如此往复。</li></ul><h3 id="结束了吗" tabindex="-1">结束了吗？ <a class="header-anchor" href="#结束了吗" aria-label="Permalink to &quot;结束了吗？&quot;">​</a></h3><p>到此就结束了吗？ 是的，http的缓存机制就是如此了，但是仍然存在一个问题：</p><p>浏览器无法主动得知服务器上的 a.js 资源变化了。</p><p>不管用 Expires 还是 Cache-Control，他们都只能够控制缓存是否过期，但是在缓存过期之前，浏览器是无法得知服务器上的资源是否变化的。只有当缓存过期后，浏览器才会发请求询问服务器。</p><h3 id="最终方案" tabindex="-1">最终方案 <a class="header-anchor" href="#最终方案" aria-label="Permalink to &quot;最终方案&quot;">​</a></h3><p>大家可以想象我们使用 a.js 的场景，我们一般都是输入网址，访问一个 html 文件，html文件中会引入 js、css 、图片等资源。</p><p>所以呢，我们在html上做些手脚。</p><p>我们不让 html 文件缓存，每次访问 html 都去请求服务器。所以浏览器每次都能拿到最新的html资源。</p><p>a.js 内容更新的时候，我们修改一下 html 中 a.js 的版本号。</p><ul><li>第一次访问 html</li></ul><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>&lt;script src=&quot;http://test.com/a.js?version=0.0.1&quot;&gt;&lt;/script&gt;</span></span>
<span class="line"><span>复制代码</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><ul><li>浏览器下载0.0.1版本的a.js文件。</li><li>浏览器再次访问 html，发现还是0.0.1版本的a.js文件，则使用本地缓存。</li><li>某一天a.js变了，我们的html文件也相应变化如下：</li></ul><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>&lt;script src=&quot;http://test.com/a.js?version=0.0.2&quot;&gt;&lt;/script&gt;</span></span>
<span class="line"><span>复制代码</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><ul><li>浏览器再次访问html，发现【<a href="https://link.juejin.cn/?target=http%3A%2F%2Ftest.com%2Fa.js%3Fversion%3D0.0.2%E3%80%91%E5%AF%B9%E5%BA%94%E7%9A%84%E8%B5%84%E6%BA%90%E6%B2%A1%E6%9C%89%E7%BC%93%E5%AD%98%EF%BC%8C%E5%88%99%E8%AF%B7%E6%B1%82%E6%9C%8D%E5%8A%A1%E5%99%A8%EF%BC%8C%E6%9C%8D%E5%8A%A1%E5%99%A8%E8%BF%94%E5%9B%9E%E6%9C%80%E6%96%B0%E7%9A%84" target="_blank" rel="noreferrer">test.com/a.js?versio…</a> a.js。</li><li>如此往复。</li></ul><p>所以，通过设置html不缓存，html引用资源内容变化则改变资源路径的方式，就解决了无法及时得知资源更新的问题。</p><p>当然除了以版本号来区分，也可以以 MD5hash 值来区分。 如</p><div class="language- vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span>&lt;script src=&quot;http://test.com/a.【hash值】.js&quot;&gt;&lt;/script&gt;</span></span>
<span class="line"><span>复制代码</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br></div></div><p>使用webpack打包的话，借助插件可以很方便的处理。</p><h3 id="除此以外的东东" tabindex="-1">除此以外的东东 <a class="header-anchor" href="#除此以外的东东" aria-label="Permalink to &quot;除此以外的东东&quot;">​</a></h3><p>Cache-Control 除了可以设置 max-age 相对过期时间以外，还可以设置成如下几种值：</p><ul><li>public，资源允许被中间服务器缓存。</li></ul><blockquote><p>浏览器请求服务器时，如果缓存时间没到，中间服务器直接返回给浏览器内容，而不必请求源服务器。</p></blockquote><ul><li>private，资源不允许被中间代理服务器缓存。</li></ul><blockquote><p>浏览器请求服务器时，中间服务器都要把浏览器的请求透传给服务器。</p></blockquote><ul><li>no-cache，浏览器不做缓存检查。</li></ul><blockquote><p>每次访问资源，浏览器都要向服务器询问，如果文件没变化，服务器只告诉浏览器继续使用缓存（304）。</p></blockquote><ul><li>no-store，浏览器和中间代理服务器都不能缓存资源。</li></ul><blockquote><p>每次访问资源，浏览器都必须请求服务器，并且，服务器不去检查文件是否变化，而是直接返回完整的资源。</p></blockquote><ul><li>must-revalidate，可以缓存，但是使用之前必须先向源服务器确认。</li><li>proxy-revalidate，要求缓存服务器针对缓存资源向源服务器进行确认。</li><li>s-maxage：缓存服务器对资源缓存的最大时间。</li></ul><p>Cache-Control 对缓存的控制粒度更细，包括缓存代理服务器的缓存控制。</p>`,146);function m(e,f,b,g,k,q){return p(),l("div",null,[a("h1",d,[t(s(e.$frontmatter.title)+" ",1),h]),u])}const M=i(c,[["render",m]]);export{E as __pageData,M as default};
