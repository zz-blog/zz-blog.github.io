import{_ as a,c as t,l as e,a as d,t as r,a4 as n,o as l}from"./chunks/framework.CzsDe1xi.js";const f=JSON.parse('{"title":"DaemonSet 详解","description":"","frontmatter":{"title":"DaemonSet 详解"},"headers":[],"relativePath":"doc/DevOps/Kubernetes/基础/DaemonSet/DaemonSet.md","filePath":"packages/doc/src/DevOps/Kubernetes/基础/DaemonSet/DaemonSet.md","lastUpdated":1705214817000}'),i={name:"doc/DevOps/Kubernetes/基础/DaemonSet/DaemonSet.md"},c={id:"frontmatter-title",tabindex:"-1"},s=e("a",{class:"header-anchor",href:"#frontmatter-title","aria-label":'Permalink to "{{ $frontmatter.title }}"'},"​",-1),p=n('<h2 id="什么是-daemonset" tabindex="-1">什么是 DaemonSet？ <a class="header-anchor" href="#什么是-daemonset" aria-label="Permalink to &quot;什么是 DaemonSet？&quot;">​</a></h2><p><em>DaemonSet</em> 确保全部（或者一些）Node 上运行一个 Pod 的副本。当有 Node 加入集群时，也会为他们新增一个 Pod 。当有 Node 从集群移除时，这些 Pod 也会被回收。删除 DaemonSet 将会删除它创建的所有 Pod。</p><p>使用 DaemonSet 的一些典型用法：</p><ul><li>运行集群存储 daemon，例如在每个 Node 上运行 <code>glusterd</code>、<code>ceph</code>。</li><li>在每个 Node 上运行日志收集 daemon，例如<code>fluentd</code>、<code>logstash</code>。</li><li>在每个 Node 上运行监控 daemon，例如 <a href="https://github.com/prometheus/node_exporter" target="_blank" rel="noreferrer">Prometheus Node Exporter</a>、<code>collectd</code>、Datadog 代理、New Relic 代理，或 Ganglia <code>gmond</code>。</li></ul><p>一个简单的用法是，在所有的 Node 上都存在一个 DaemonSet，将被作为每种类型的 daemon 使用。 一个稍微复杂的用法可能是，对单独的每种类型的 daemon 使用多个 DaemonSet，但具有不同的标志，和/或对不同硬件类型具有不同的内存、CPU要求。</p><h2 id="特点" tabindex="-1">特点 <a class="header-anchor" href="#特点" aria-label="Permalink to &quot;特点&quot;">​</a></h2><ul><li><p>只在特定的节点上运行Pod：可以使用NodeSelector或者NodeAffinity来限制DaemonSet的Pod只在特定的节点上运行。</p></li><li><p>根据节点的标签更新Pod：如果在集群中添加或删除了节点，Kubernetes会通过DaemonSet自动添加或删除Pod。同时，也可以通过添加或移除节点标签来更新DaemonSet中的Pod。</p></li><li><p>确保每个节点只运行一个Pod：可以使用PodAntiAffinity来确保每个节点上只有一个Pod在运行。</p></li><li><p>限制DaemonSet的Pod数量：可以使用MaxUnavailable和MaxSurge字段来限制DaemonSet的Pod的最大数量和最小数量。</p></li><li><p>DaemonSet是一种非常有用的控制器，可以确保集群中每个节点上都有一个Pod副本在运行。 它可以自动地根据节点的状态添加或删除Pod，并且可以通过各种方式来控制Pod的位置和数量。</p></li></ul><h2 id="工作原理" tabindex="-1">工作原理 <a class="header-anchor" href="#工作原理" aria-label="Permalink to &quot;工作原理&quot;">​</a></h2><p>DaemonSet的工作原理是监听节点的变化，通过监听节点的变化，当新的节点加入集群时，DaemonSet会自动在该节点上创建一个Pod副本。而当节点从集群中删除时，DaemonSet会自动删除该节点上的Pod副本。</p><p>这样，DaemonSet保证了集群中每个节点都会运行指定的Pod。有且只有一个pod</p><p>DaemonSet的工作原理如下：</p><ul><li>控制器监视节点的状态：DaemonSet控制器会监视集群中的节点状态，一旦有新的节点加入集群，或者节点状态发生变化（例如节点重新启动），控制器就会触发一些操作。</li><li>创建Pod：当控制器检测到新节点时，它会创建一个新的Pod，并将其调度到该节点上。控制器还会确保每个节点上只运行一个Pod实例。</li><li>更新Pod：如果DaemonSet的配置发生变化，例如更新了镜像版本或者修改了Pod的配置文件，控制器会自动更新每个节点上的Pod实例。</li><li>删除Pod：如果节点发生故障或者被删除，控制器会自动删除节点上的Pod实例。</li><li>扩容和缩容：DaemonSet还支持扩容和缩容，可以根据需要增加或减少Pod的数量。扩容和缩容的过程与其他控制器类似，控制器会根据指定的副本数和当前的实际Pod数量来调整Pod的数量。</li></ul><h2 id="场景介绍" tabindex="-1">场景介绍 <a class="header-anchor" href="#场景介绍" aria-label="Permalink to &quot;场景介绍&quot;">​</a></h2><p>DaemonSet用于在集群中运行一组Pod，确保每个节点都有一个Pod在运行。它通常用于运行一些系统级别的服务或者监控应用程序，例如：</p><ul><li><p>日志收集器：DaemonSet可以在每个节点上运行日志收集器，例如Fluentd或者Filebeat，从而收集所有节点的日志数据，并将其发送到中心日志服务器进行存储和分析。</p></li><li><p>监控代理：DaemonSet可以在每个节点上运行监控代理，例如Prometheus Node Exporter或者cAdvisor，从而收集所有节点的运行状态数据，并将其发送到中心监控服务器进行分析和展示。</p></li><li><p>网络代理：DaemonSet可以在每个节点上运行网络代理，例如kube-proxy或者Istio Sidecar，从而负责节点之间的网络通信和流量管理。</p></li><li><p>安全代理：DaemonSet可以在每个节点上运行安全代理，例如Sysdig Falco或者Aqua Security，从而检测所有节点的安全事件，并及时报警或者进行防御。</p></li></ul><p>总之，DaemonSet适用于需要在每个节点上运行一组Pod的场景，可以使集群中的服务更加健壮和可靠。</p><p>Daemon Pod机制听起来很简单，但的意义确实是非常重要的。列举几个例子：</p><ul><li><p>各种网络插件的 Agent 组件，都必须运行在每一个节点上，用来处理这个节点上的容器网络；</p></li><li><p>各种存储插件的 Agent 组件，也必须运行在每一个节点上，用来在这个节点上挂载远程存储目录，操作容器的 Volume 目录；</p></li><li><p>各种监控组件和日志组件，也必须运行在每一个节点上，负责这个节点上的监控信息和日志搜集。</p></li></ul><h2 id="编写-daemonset-spec" tabindex="-1">编写 DaemonSet Spec <a class="header-anchor" href="#编写-daemonset-spec" aria-label="Permalink to &quot;编写 DaemonSet Spec&quot;">​</a></h2><h3 id="必需字段" tabindex="-1">必需字段 <a class="header-anchor" href="#必需字段" aria-label="Permalink to &quot;必需字段&quot;">​</a></h3><p>和其它所有 Kubernetes 配置一样，DaemonSet 需要 <code>apiVersion</code>、<code>kind</code> 和 <code>metadata</code>字段。有关配置文件的通用信息，详见文档 <a href="https://kubernetes.io/docs/user-guide/deploying-applications/" target="_blank" rel="noreferrer">部署应用</a>、<a href="https://kubernetes.io/docs/user-guide/configuring-containers/" target="_blank" rel="noreferrer">配置容器</a> 和资源管理。</p><p>DaemonSet 也需要一个 <code>.spec</code>配置段。</p><h3 id="pod-模板" tabindex="-1">Pod 模板 <a class="header-anchor" href="#pod-模板" aria-label="Permalink to &quot;Pod 模板&quot;">​</a></h3><p><code>.spec</code> 唯一必需的字段是 <code>.spec.template</code>。</p><p><code>.spec.template</code> 是一个 <a href="https://kubernetes.io/docs/user-guide/replication-controller/#pod-template" target="_blank" rel="noreferrer">Pod 模板</a>。 它与 <a href="https://kubernetes.io/docs/user-guide/pods" target="_blank" rel="noreferrer">Pod</a> 具有相同的 schema，除了它是嵌套的，而且不具有 <code>apiVersion</code> 或 <code>kind</code> 字段。</p><p>Pod 除了必须字段外，在 DaemonSet 中的 Pod 模板必须指定合理的标签（查看 <a href="#pod-selector">pod selector</a>）。</p><p>在 DaemonSet 中的 Pod 模板必需具有一个值为 <code>Always</code> 的 <a href="https://kubernetes.io/docs/user-guide/pod-states" target="_blank" rel="noreferrer"><code>RestartPolicy</code></a>，或者未指定它的值，默认是 <code>Always</code>。</p><h3 id="pod-selector" tabindex="-1">Pod Selector <a class="header-anchor" href="#pod-selector" aria-label="Permalink to &quot;Pod Selector&quot;">​</a></h3><p><code>.spec.selector</code> 字段表示 Pod Selector，它与 <a href="https://kubernetes.io/docs/concepts/jobs/run-to-completion-finite-workloads/" target="_blank" rel="noreferrer">Job</a> 或其它资源的 <code>.spec.selector</code> 的原理是相同的。</p><p><code>spec.selector</code> 表示一个对象，它由如下两个字段组成：</p><ul><li><code>matchLabels</code> - 与 <a href="https://kubernetes.io/docs/concepts/workloads/controllers/replicationcontroller/" target="_blank" rel="noreferrer">ReplicationController</a> 的 <code>.spec.selector</code> 的原理相同。</li><li><code>matchExpressions</code> - 允许构建更加复杂的 Selector，可以通过指定 key、value 列表，以及与 key 和 value 列表的相关的操作符。</li></ul><p>当上述两个字段都指定时，结果表示的是 AND 关系。</p><p>如果指定了 <code>.spec.selector</code>，必须与 <code>.spec.template.metadata.labels</code> 相匹配。如果没有指定，它们默认是等价的。如果与它们配置的不匹配，则会被 API 拒绝。</p><p>如果 Pod 的 label 与 selector 匹配，或者直接基于其它的 DaemonSet、或者 Controller（例如 ReplicationController），也不可以创建任何 Pod。 否则 DaemonSet Controller 将认为那些 Pod 是它创建的。Kubernetes 不会阻止这样做。一个场景是，可能希望在一个具有不同值的、用来测试用的 Node 上手动创建 Pod。</p><h3 id="仅在相同的-node-上运行-pod" tabindex="-1">仅在相同的 Node 上运行 Pod <a class="header-anchor" href="#仅在相同的-node-上运行-pod" aria-label="Permalink to &quot;仅在相同的 Node 上运行 Pod&quot;">​</a></h3><p>如果指定了 <code>.spec.template.spec.nodeSelector</code>，DaemonSet Controller 将在能够匹配上 <a href="https://kubernetes.io/docs/concepts/configuration/assign-pod-node/" target="_blank" rel="noreferrer">Node Selector</a> 的 Node 上创建 Pod。 类似这种情况，可以指定 <code>.spec.template.spec.affinity</code>，然后 DaemonSet Controller 将在能够匹配上 <a href="https://kubernetes.io/docs/concepts/configuration/assign-pod-node/" target="_blank" rel="noreferrer">Node Affinity</a> 的 Node 上创建 Pod。 如果根本就没有指定，则 DaemonSet Controller 将在所有 Node 上创建 Pod。</p><h2 id="如何调度-daemon-pod" tabindex="-1">如何调度 Daemon Pod <a class="header-anchor" href="#如何调度-daemon-pod" aria-label="Permalink to &quot;如何调度 Daemon Pod&quot;">​</a></h2><p>正常情况下，Pod 运行在哪个机器上是由 Kubernetes 调度器进行选择的。然而，由 Daemon Controller 创建的 Pod 已经确定了在哪个机器上（Pod 创建时指定了 <code>.spec.nodeName</code>），因此：</p><ul><li>DaemonSet Controller 并不关心一个 Node 的 <a href="https://kubernetes.io/docs/admin/node/#manual-node-administration" target="_blank" rel="noreferrer"><code>unschedulable</code></a> 字段。</li><li>DaemonSet Controller 可以创建 Pod，即使调度器还没有被启动，这对集群启动是非常有帮助的。</li></ul><p>Daemon Pod 关心 <a href="https://kubernetes.io/docs/concepts/configuration/assign-pod-node/#taints-and-tolerations-beta-feature" target="_blank" rel="noreferrer">Taint 和 Toleration</a>，它们会为没有指定 <code>tolerationSeconds</code> 的 <code>node.alpha.kubernetes.io/notReady</code> 和 <code>node.alpha.kubernetes.io/unreachable</code> 的 Taint，而创建具有 <code>NoExecute</code> 的 Toleration。这确保了当 alpha 特性的 <code>TaintBasedEvictions</code> 被启用，当 Node 出现故障，比如网络分区，这时它们将不会被清除掉（当 <code>TaintBasedEvictions</code> 特性没有启用，在这些场景下也不会被清除，但会因为 NodeController 的硬编码行为而被清除，Toleration 是不会的）。</p><h2 id="与-daemon-pod-通信" tabindex="-1">与 Daemon Pod 通信 <a class="header-anchor" href="#与-daemon-pod-通信" aria-label="Permalink to &quot;与 Daemon Pod 通信&quot;">​</a></h2><p>与 DaemonSet 中的 Pod 进行通信，几种可能的模式如下：</p><ul><li><strong>Push</strong>：配置 DaemonSet 中的 Pod 向其它 Service 发送更新，例如统计数据库。它们没有客户端。</li><li><strong>NodeIP 和已知端口</strong>：DaemonSet 中的 Pod 可以使用 <code>hostPort</code>，从而可以通过 Node IP 访问到 Pod。客户端能通过某种方法知道 Node IP 列表，并且基于此也可以知道端口。</li><li><strong>DNS</strong>：创建具有相同 Pod Selector 的 <a href="https://kubernetes.io/docs/user-guide/services/#headless-services" target="_blank" rel="noreferrer">Headless Service</a>，然后通过使用 <code>endpoints</code> 资源或从 DNS 检索到多个 A 记录来发现 DaemonSet。</li><li><strong>Service</strong>：创建具有相同 Pod Selector 的 Service，并使用该 Service 访问到某个随机 Node 上的 daemon。（没有办法访问到特定 Node）</li></ul><h2 id="更新-daemonset" tabindex="-1">更新 DaemonSet <a class="header-anchor" href="#更新-daemonset" aria-label="Permalink to &quot;更新 DaemonSet&quot;">​</a></h2><p>如果修改了 Node Label，DaemonSet 将立刻向新匹配上的 Node 添加 Pod，同时删除新近无法匹配上的 Node 上的 Pod。</p><p>可以修改 DaemonSet 创建的 Pod。然而，不允许对 Pod 的所有字段进行更新。当下次 Node（即使具有相同的名称）被创建时，DaemonSet Controller 还会使用最初的模板。</p><p>可以删除一个 DaemonSet。如果使用 <code>kubectl</code> 并指定 <code>--cascade=false</code> 选项，则 Pod 将被保留在 Node 上。然后可以创建具有不同模板的新 DaemonSet。具有不同模板的新 DaemonSet 将鞥能够通过 Label 匹配识别所有已经存在的 Pod。它不会修改或删除它们，即使是错误匹配了 Pod 模板。通过删除 Pod 或者 删除 Node，可以强制创建新的 Pod。</p><p>在 Kubernetes 1.6 或以后版本，可以在 DaemonSet 上 <a href="https://kubernetes.io/docs/tasks/manage-daemon/update-daemon-set/" target="_blank" rel="noreferrer">执行滚动升级</a>。</p><h3 id="init-脚本" tabindex="-1">init 脚本 <a class="header-anchor" href="#init-脚本" aria-label="Permalink to &quot;init 脚本&quot;">​</a></h3><p>很可能通过直接在一个 Node 上启动 daemon 进程（例如，使用 <code>init</code>、<code>upstartd</code>、或 <code>systemd</code>）。这非常好，然而基于 DaemonSet 来运行这些进程有如下一些好处：</p><ul><li>像对待应用程序一样，具备为 daemon 提供监控和管理日志的能力。</li><li>为 daemon 和应用程序使用相同的配置语言和工具（如 Pod 模板、<code>kubectl</code>）。</li><li>Kubernetes 未来版本可能会支持对 DaemonSet 创建 Pod 与 Node升级工作流进行集成。</li><li>在资源受限的容器中运行 daemon，能够增加 daemon 和应用容器的隔离性。然而这也实现了在容器中运行 daemon，但却不能在 Pod 中运行（例如，直接基于 Docker 启动）。</li></ul><h3 id="裸-pod" tabindex="-1">裸 Pod <a class="header-anchor" href="#裸-pod" aria-label="Permalink to &quot;裸 Pod&quot;">​</a></h3><p>可能要直接创建 Pod，同时指定其运行在特定的 Node 上。 然而，DaemonSet 替换了由于任何原因被删除或终止的 Pod，例如 Node 失败、例行节点维护，比如内核升级。由于这个原因，我们应该使用 DaemonSet 而不是单独创建 Pod。</p><h3 id="静态-pod" tabindex="-1">静态 Pod <a class="header-anchor" href="#静态-pod" aria-label="Permalink to &quot;静态 Pod&quot;">​</a></h3><p>很可能，通过在一个指定目录下编写文件来创建 Pod，该目录受 Kubelet 所监视。这些 Pod 被称为 <a href="https://kubernetes.io/docs/concepts/cluster-administration/static-pod/" target="_blank" rel="noreferrer">静态 Pod</a>。 不像 DaemonSet，静态 Pod 不受 kubectl 和 其它 Kubernetes API 客户端管理。静态 Pod 不依赖于 apiserver，这使得它们在集群启动的情况下非常有用。 而且，未来静态 Pod 可能会被废弃掉。</p><h3 id="replication-controller" tabindex="-1">Replication Controller <a class="header-anchor" href="#replication-controller" aria-label="Permalink to &quot;Replication Controller&quot;">​</a></h3><p>DaemonSet 与 <a href="https://kubernetes.io/docs/user-guide/replication-controller" target="_blank" rel="noreferrer">Replication Controller</a> 非常类似，它们都能创建 Pod，这些 Pod 都具有不期望被终止的进程（例如，Web 服务器、存储服务器）。 为无状态的 Service 使用 Replication Controller，像 frontend，实现对副本的数量进行扩缩容、平滑升级，比之于精确控制 Pod 运行在某个主机上要重要得多。需要 Pod 副本总是运行在全部或特定主机上，并需要先于其他 Pod 启动，当这被认为非常重要时，应该使用 Daemon Controller。</p>',57);function m(o,h,P,u,S,b){return l(),t("div",null,[e("h1",c,[d(r(o.$frontmatter.title)+" ",1),s]),p])}const k=a(i,[["render",m]]);export{f as __pageData,k as default};
