import{_ as e,c as a,o as s,a5 as o}from"./chunks/framework.DwbewbAn.js";const y=JSON.parse('{"title":"Gateway","description":"","frontmatter":{},"headers":[],"relativePath":"doc/DevOps/Kubernetes/基础/gateway.md","filePath":"packages/doc/src/DevOps/Kubernetes/基础/gateway.md","lastUpdated":1709020051000}'),i={name:"doc/DevOps/Kubernetes/基础/gateway.md"},t=o(`<h1 id="gateway" tabindex="-1">Gateway <a class="header-anchor" href="#gateway" aria-label="Permalink to &quot;Gateway&quot;">​</a></h1><p>除了直接使用 Service 和 Ingress 之外，Kubernetes 社区还发起了 <a href="https://github.com/kubernetes-sigs/gateway-api" target="_blank" rel="noreferrer">Gateway API 项目</a>，这是一个 CRD，可以帮助我们将 Kubernetes 中的服务暴露到集群外。</p><p>Gateway API 是一个由 <a href="https://github.com/kubernetes/community/tree/master/sig-network" target="_blank" rel="noreferrer">SIG-NETWORK</a> 管理的开源项目。该项目的目标是在 Kubernetes 生态系统中发展服务网络 API。Gateway API 提供了暴露 Kubernetes 应用的接口 ——Service、Ingress 等。</p><p>该 API 在 Istio 中也被应用，用于将 Kubernetes 中的服务暴露到服务网格之外。</p><h2 id="目标" tabindex="-1">目标 <a class="header-anchor" href="#目标" aria-label="Permalink to &quot;目标&quot;">​</a></h2><p>Gateway API 旨在通过提供表现性的、可扩展的、面向角色的接口来改善服务网络，这些接口由许多厂商实现，并得到了业界的广泛支持。</p><p>Gateway API 是一个 API 资源的集合 —— Service、GatewayClass、Gateway、HTTPRoute、TCPRoute 等。使用这些资源共同为各种网络用例建模。</p><p>下图中展示的是 Kubernetes 集群中四层和七层的网络配置。从图中可以看到通过将这些资源对象分离，可以实现配置上的解耦，由不同角色的人员来管理。</p><h2 id="gateway-相较于-ingress-做了哪些改进" tabindex="-1">Gateway 相较于 Ingress 做了哪些改进？ <a class="header-anchor" href="#gateway-相较于-ingress-做了哪些改进" aria-label="Permalink to &quot;Gateway 相较于 Ingress 做了哪些改进？&quot;">​</a></h2><p><strong>更具表现力</strong></p><p>Gateway 表达了更多的核心功能，比如基于头的匹配、流量加权和其他功能，而这些功能在 Ingress 中只能通过自定义方式实现。</p><p><strong>更具扩展性</strong></p><p>Gateway API 允许在 API 的各个层次上链接自定义资源。这就允许在 API 结构的适当位置进行更精细的定制。</p><p><strong>面向角色</strong></p><p>它们被分离成不同的 API 资源，这些资源映射到 Kubernetes 上运行应用程序的常见角色。</p><p><strong>通用性</strong></p><p>这不是一种改进，而是应该保持不变。正如 Ingress 是一个具有众多实现的通用规范一样，Gateway API 被设计成一个由许多实现支持的可移植规范。</p><p><strong>共享网关</strong></p><p>它们允许独立的路由资源绑定到同一个网关，从而实现负载均衡器和 VIP 的共享。这允许团队安全地共享基础设施，而不需要直接协调。</p><p><strong>类型化后端引用</strong></p><p>通过类型化后端引用，Routes 可以引用 Kubernetes 服务，也可以引用任何一种被设计为 Gateway 后端的 Kubernetes 资源。</p><p><strong>跨命名空间引用</strong></p><p>跨越不同 Namespaces 的路由可以绑定到网关。这样，尽管对工作负载进行了命名空间划分，但仍可共享网络基础设施。</p><p><strong>类</strong></p><p><code>GatewayClasses</code> 将负载均衡实现的类型形式化。这些类使用户可以很容易和明确地了解资源模型本身有什么样的能力。</p><p>在了解了 Gateway API 的目的后，接下来我们再看下它的资源模型、请求流程、TLS 配置及扩展点等。</p><h2 id="角色" tabindex="-1">角色 <a class="header-anchor" href="#角色" aria-label="Permalink to &quot;角色&quot;">​</a></h2><p>Gateway API 开发者为其使用场景定义四类角色：</p><ul><li>基础设施提供方：如 AWS、GKE 等</li><li>集群运维：管理整个集群的计算、存储、网络、安全等</li><li>应用程序开发者：为自己开发的应用负责，管理应用的健壮性</li><li>应用管理员：不是所有的公司都有，通常在一些复杂系统中会有专门的应用管理员</li></ul><h2 id="资源模型" tabindex="-1">资源模型 <a class="header-anchor" href="#资源模型" aria-label="Permalink to &quot;资源模型&quot;">​</a></h2><p>注意：资源最初将作为 CRD 存在于 <code>networking.x-k8s.io</code> API 组中。未限定的资源名称将隐含在该 API 组中。</p><p>Gateway API 的资源模型中，主要有三种类型的对象：</p><ul><li><code>GatewayClass</code>：定义了一组具有共同配置和行为的网关。</li><li><code>Gateway</code>：请求一个点，在这个点上，流量可以被翻译到集群内的服务。</li><li><code>Route</code>：描述了通过 Gateway 而来的流量如何映射到服务。</li></ul><h3 id="gatewayclass" tabindex="-1">GatewayClass <a class="header-anchor" href="#gatewayclass" aria-label="Permalink to &quot;GatewayClass&quot;">​</a></h3><p><code>GatewayClass</code> 定义了一组共享共同配置和行为的 Gateway，每个 <code>GatewayClass</code> 由一个控制器处理，但控制器可以处理多个 <code>GatewayClass</code>。</p><p><code>GatewayClass</code> 是一个集群范围的资源。必须至少定义一个 <code>GatewayClass</code>，<code>Gateway</code> 才能够生效。实现 Gateway API 的控制器通过关联的 <code>GatewayClass</code> 资源来实现，用户可以在自己的 <code>Gateway</code> 中引用该资源。</p><p>这类似于 <code>Ingress</code> 的 <a href="https://github.com/kubernetes/enhancements/blob/master/keps/sig-network/20190125-ingress-api-group.md#ingress-class" target="_blank" rel="noreferrer"><code>IngressClass</code></a> 和 <code>PersistentVolumes</code> 的 <a href="https://kubernetes.io/docs/concepts/storage/storage-classes/" target="_blank" rel="noreferrer"><code>StorageClass</code></a>。在 <code>Ingress</code> v1beta1 中，最接近 <code>GatewayClass</code> 的是 <code>ingress-class</code> 注解，而在 IngressV1 中，最接近的类似物是 <code>IngressClass</code> 对象。</p><h3 id="gateway-1" tabindex="-1">Gateway <a class="header-anchor" href="#gateway-1" aria-label="Permalink to &quot;Gateway&quot;">​</a></h3><p><code>Gateway</code> 描述了如何将流量翻译到集群内的服务。也就是说，它定义了一个方法，将流量从不了解 Kubernetes 的地方翻译到了解 Kubernetes 的地方。例如，由云负载均衡器、集群内代理或外部硬件负载均衡器发送到 Kubernetes 服务的流量。虽然许多用例的客户端流量源自集群的 &quot;外部&quot;，但这并不强求。</p><p><code>Gateway</code> 定义了对实现 <code>GatewayClass</code> 配置和行为合同的特定负载均衡器配置的请求。该资源可以由运维人员直接创建，也可以由处理 <code>GatewayClass</code> 的控制器创建。</p><p>由于 <code>Gateway</code> 规范捕获了用户意图，它可能不包含规范中所有属性的完整规范。例如，用户可以省略地址、端口、TLS 设置等字段。这使得管理 <code>GatewayClass</code> 的控制器可以为用户提供这些设置，从而使规范更加可移植。这种行为将通过 <code>GatewayClass</code> 状态对象来明确。</p><p>一个 <code>Gateway</code> 可以包含一个或多个 <code>Route</code> 引用，这些 <code>Route</code> 引用的作用是将一个子集的流量引导到一个特定的服务上。</p><h3 id="http-tcp-foo-route" tabindex="-1">{HTTP,TCP,Foo} Route <a class="header-anchor" href="#http-tcp-foo-route" aria-label="Permalink to &quot;{HTTP,TCP,Foo} Route&quot;">​</a></h3><p><code>Route</code> 对象定义了特定协议的规则，用于将请求从 <code>Gateway</code> 映射到 Kubernetes 服务。</p><p><code>HTTPRoute</code> 和 <code>TCPRoute</code> 是目前唯一已定义的 <code>Route</code> 对象。未来可能会添加其他特定协议的 <code>Route</code> 对象。</p><h3 id="backendpolicy" tabindex="-1">BackendPolicy <a class="header-anchor" href="#backendpolicy" aria-label="Permalink to &quot;BackendPolicy&quot;">​</a></h3><p><code>BackendPolicy</code> 提供了一种配置 <code>Gateway</code> 和后端之间连接的方法。在这个 API 中，后端是指路由可以转发流量的任何资源。后端的一个常见例子是 <code>Service</code>。这个级别的配置目前仅限于 TLS，但将来会扩展到支持更高级的策略，如健康检查。</p><p>一些后端配置可能会根据针对后端的 <code>Route</code> 而有所不同。在这些情况下，配置字段将放在 <code>Route</code> 上，而不是 <code>BackendPolicy</code> 上。有关该资源未来可能配置的更多信息，请参考相关的 <a href="https://github.com/kubernetes-sigs/gateway-apis/issues/196" target="_blank" rel="noreferrer">GitHub issue</a>。</p><h3 id="路由绑定" tabindex="-1">路由绑定 <a class="header-anchor" href="#路由绑定" aria-label="Permalink to &quot;路由绑定&quot;">​</a></h3><p>当 <code>Route</code> 绑定到 <code>Gateway</code> 时，代表应用在 <code>Gateway</code> 上的配置，配置了底层的负载均衡器或代理。哪些 <code>Route</code> 如何绑定到 <code>Gateway</code> 是由资源本身控制的。<code>Route</code> 和 <code>Gateway</code> 资源具有内置的控制，以允许或限制它们之间如何相互选择。这对于强制执行组织政策以确定 <code>Route</code> 如何暴露以及在哪些 <code>Gateway</code> 上暴露非常有用。看下下面的例子。</p><p>一个 Kubernetes 集群管理员在 <code>Infra</code> 命名空间中部署了一个名为 <code>shared-gw</code> 的 <code>Gateway</code>，供不同的应用团队使用，以便将其应用暴露在集群之外。团队 A 和团队 B（分别在命名空间 &quot;A&quot; 和 &quot;B&quot; 中）将他们的 <code>Route</code> 绑定到这个 <code>Gateway</code>。它们互不相识，只要它们的 <code>Route</code> 规则互不冲突，就可以继续隔离运行。团队 C 有特殊的网络需求（可能是性能、安全或关键性），他们需要一个专门的 <code>Gateway</code> 来代理他们的应用到集群外。团队 C 在 &quot;C&quot; 命名空间中部署了自己的 <code>Gateway</code> <code>specialive-gw</code>，该 Gateway 只能由 &quot;C&quot; 命名空间中的应用使用。</p><p>不同命名空间及 <code>Gateway</code> 与 <code>Route</code> 的绑定关系如下图所示。</p><p>在如何将路由与网关绑定以实现不同的组织政策和责任范围方面，有很大的灵活性。下面是网关和路由之间可能的对应关系：</p><ul><li>一对一：网关和路由可以由一个所有者部署和使用，并具有一对一的关系。团队 C 就是一个例子。</li><li>一对多：一个网关可以有许多路由与之绑定，这些路由由来自不同命名空间的不同团队所拥有。团队 A 和 B 就是这样的一个例子。</li><li>多对一：路由也可以绑定到多个网关，允许一个路由同时控制不同 IP、负载均衡器或网络上的应用暴露。</li></ul><p>总之，网关选择路由，路由控制它们的暴露。当网关选择一个允许自己暴露的路由时，那么该路由将与网关绑定。当路由与网关绑定时，意味着它们的集体路由规则被配置在了由该网关管理的底层负载均衡器或代理服务器上。因此，网关是一个网络数据平面的逻辑表示，可以通过路由进行配置。</p><h4 id="路由选择" tabindex="-1">路由选择 <a class="header-anchor" href="#路由选择" aria-label="Permalink to &quot;路由选择&quot;">​</a></h4><p><code>Gateway</code> 根据 <code>Route</code> 元数据，特别是 <code>Route</code> 资源的种类、命名空间和标签来选择 <code>Route</code>。<code>Route</code> 实际上被绑定到 <code>Gateway</code> 中的特定监听器上，因此每个监听器都有一个 <code>listener.routes</code> 字段，它通过以下一个或多个标准来选择 <code>Route</code>。</p><ul><li><strong>Label</strong>：<code>Gateway</code> 可以通过资源上存在的标签来选择 <code>Route</code>（类似于 Service 通过 Pod 标签选择 Pod 的方式）。</li><li><strong>Kind</strong>：网关监听器只能选择单一类型的路由资源。可以是 <code>HTTPRoute</code>、<code>TCPRoute</code> 或自定义 <code>Route</code> 类型。</li><li><strong>Namespace</strong>：<code>Gateway</code> 还可以通过 <code>namespaces.from</code> 字段控制可以从哪些 <code>Namespace</code>、 <code>Route</code> 中选择。它支持三种可能的值。 <ul><li><code>SameNamespace</code> 是默认选项。只有与该网关相同的命名空间中的路由才会被选择。</li><li><code>All</code> 将选择来自所有命名空间的 <code>Route</code>。</li><li><code>Selector</code> 意味着该网关将选择由 Namespace 标签选择器选择的 Namespace 子集的 Route。当使用 Selector 时，那么 <code>listeners.route.namespaces.selector</code> 字段可用于指定标签选择器。<code>All</code> 或 <code>SameNamespace</code> 不支持该字段。</li></ul></li></ul><p>下面的 <code>Gateway</code> 将在集群中的所有 <code>Namespace</code> 中选择 <code>expose: prod-web-gw</code> 的所有 <code>HTTPRoute</code> 资源。</p><div class="language-yaml vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">kind</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">Gateway</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">...</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">spec</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  listeners</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:  </span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">routes</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      kind</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">HTTPRoute</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      selector</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        matchLabels</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">          expose</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">prod-web-gw</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> </span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      namespaces</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">        from</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">All</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br><span class="line-number">11</span><br></div></div><h4 id="路由暴露" tabindex="-1">路由暴露 <a class="header-anchor" href="#路由暴露" aria-label="Permalink to &quot;路由暴露&quot;">​</a></h4><p>路由可以决定它们如何通过网关暴露。<code>gateways.allow</code> 字段支持三个值。</p><ul><li><code>All</code>：如果没有指定，则是默认值。这使得所有的 <code>Route</code> 标签和 <code>Namespace</code> 选择器都绑定在网关上。</li><li><code>SameNamespace</code> 只允许该路由与来自同一 <code>Namespace</code> 的网关绑定。</li><li><code>FromList</code> 允许指定一个明确的网关列表，以便路由与之绑定。</li></ul><p>下面的 <code>my-route</code> Route 只选择 <code>foo-namespace</code> 中的 <code>foo-gateway</code>，而不能与其他 <code>Gateway</code> 绑定。注意，<code>foo-gateway</code> 与 <code>my-route</code> 在不同的 Namespace 中。如果 <code>foo-gateway</code> 允许跨 <code>Namespace</code> 绑定，并且也选择了这个 <code>Route</code>，那么 <code>my-route</code> 就会与之绑定。</p><div class="language-yaml vp-adaptive-theme line-numbers-mode"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">kind</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">HTTPRoute</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">metadata</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">my-route</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  namespace</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">bar-namespace</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">spec</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">  gateways</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    allow</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">FromList</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    gatewayRefs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">foo-gateway</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">      namespace</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">foo-namespace</span></span></code></pre><div class="line-numbers-wrapper" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br><span class="line-number">10</span><br></div></div><p>请注意，网关和路由的绑定是双向的。这意味着两个资源必须相互选择才能绑定。如果一个<code>Gateway</code>的<code>Route</code>标签选择器不匹配任何现有的<code>Route</code>，那么即使<code>Route</code>的<code>spec.gateways.allow = All</code>，也不会有任何东西与之绑定。同样，如果一个<code>Route</code>引用了一个特定的<code>Gateway</code>，但该<code>Gateway</code>没有选择<code>Route</code>的<code>Namespace</code>，那么它们也不会绑定。只有当两个资源相互选择时，才会发生绑定。</p><p>从资源规范中可能并不总是能明显看出哪些网关和路由是绑定的，但可以通过资源状态来确定绑定。路由状态将列出路由所绑定的所有网关以及绑定的任何相关条件。</p><h3 id="组合类型" tabindex="-1">组合类型 <a class="header-anchor" href="#组合类型" aria-label="Permalink to &quot;组合类型&quot;">​</a></h3><p><code>GatewayClass</code>、Gateway、<code>xRoute</code> 和 <code>Service</code> 的组合将定义一个可实现的负载均衡器。下图说明了不同资源之间的关系。</p><h2 id="请求流程" tabindex="-1">请求流程 <a class="header-anchor" href="#请求流程" aria-label="Permalink to &quot;请求流程&quot;">​</a></h2><p>使用反向代理实现的网关的一个典型的客户端 / 网关 API 请求流程是：</p><ul><li>客户端向 <code>http://foo.example.com</code> 发出请求。</li><li>DNS 将该名称解析为网关地址。</li><li>反向代理在 <code>Listener</code> 上接收请求，并使用 <code>Host</code> 头 来匹配 <code>HTTPRoute</code>。</li><li>可选地，反向代理可以根据 <code>HTTPRoute</code> 的匹配规则执行请求头和 / 或路径匹配。</li><li>可选地，反向代理可以根据 <code>HTTPRoute</code> 的过滤规则修改请求，即添加 / 删除头。</li><li>最后，反向代理可以根据 <code>HTTPRoute</code> 的 <code>forwardTo</code> 规则，将请求转发到集群中的一个或多个对象，即 <code>Service</code>。</li></ul><h2 id="tls-配置" tabindex="-1">TLS 配置 <a class="header-anchor" href="#tls-配置" aria-label="Permalink to &quot;TLS 配置&quot;">​</a></h2><p>TLS 配置在 <code>Gateway</code> 监听器上。此外，对于某些自助服务用例，TLS 证书可以配置在路由对象上。</p><h2 id="扩展点" tabindex="-1">扩展点 <a class="header-anchor" href="#扩展点" aria-label="Permalink to &quot;扩展点&quot;">​</a></h2><p>API 中提供了一些扩展点，以灵活处理大量通用 API 无法处理的用例。</p><p>以下是 API 中扩展点的摘要。</p><ul><li><code>XRouteMatch.ExtensionRef</code>：这个扩展点应该用来扩展特定核心 <code>Route</code> 的匹配语义。这是一个实验性的扩展点，未来会根据反馈进行迭代。</li><li><code>XForwardTo.BackendRef</code>：这个扩展点应该用于将流量转发到核心 Kubernetes 服务资源以外的网络端点。例如 S3 bucket、Lambda 函数、文件服务器等。</li><li><code>HTTPRouteFilter</code>：<code>HTTPRoute</code> 为这一 API 类型提供了一种方法，可以钩入 HTTP 请求的请求 / 响应生命周期。</li><li>自定义路由：如果上述扩展点都不能满足用例的需求，实现者可以选择为目前 API 中不支持的协议创建自定义路由资源。</li></ul><h2 id="参考" tabindex="-1">参考 <a class="header-anchor" href="#参考" aria-label="Permalink to &quot;参考&quot;">​</a></h2><ul><li><a href="https://github.com/kubernetes-sigs/gateway-api" target="_blank" rel="noreferrer">kuberentes-sigs/gateway-api - github.com</a></li></ul>`,80),n=[t];function c(l,d,p,r,h,k){return s(),a("div",null,n)}const g=e(i,[["render",c]]);export{y as __pageData,g as default};
