import{_ as p,o as e,c as o,Q as t}from"./chunks/framework.1b6086f5.js";const M=JSON.parse('{"title":"","description":"","frontmatter":{},"headers":[],"relativePath":"doc/业务框架/DTM/mysql的XA.md","filePath":"packages/doc/src/业务框架/DTM/mysql的XA.md","lastUpdated":1702458087000}'),r={name:"doc/业务框架/DTM/mysql的XA.md"},s=t('<p>XA 是由 X/Open 组织提出的分布式事务规范，XA 规范主要定义了事务协调者（Transaction Manager）和资源管理器（Resource Manager）之间的接口。</p><p>​ <img src="https://note.youdao.com/yws/public/resource/4248ff5f4022101b536d851e3e06638c/xmlnote/E1FE49C6A7F64733AF7640FC5D5EC077/30470" alt="0"></p><p><strong>事务协调者（Transaction Manager）</strong></p><p>因为 XA 事务是基于两阶段提交协议的，所以需要有一个协调者，来保证所有的事务参与者都完成了准备工作，也就是 2PC 的第一阶段。</p><p>如果事务协调者收到所有参与者都准备好的消息，就会通知所有的事务都可以提交，也就是 2PC 的第二阶段。之所以需要引入事务协调者，是因为在分布式系统中，两台机器理论上无法达到一致的状态，需要引入一个单点进行协调。</p><p><strong>资源管理器（Resource Manager）</strong></p><p>负责控制和管理实际资源，比如数据库或 JMS 队列。</p><p>目前，主流数据库都提供了对 XA 的支持，在 JMS 规范中，即 Java 消息服务（Java Message Service）中，也基于 XA 定义了对事务的支持。</p><p><strong>XA 事务的执行流程</strong></p><p>XA 事务是两阶段提交的一种实现方式，根据 2PC 的规范，XA 将一次事务分割成了两个阶段，即 Prepare 和 Commit 阶段。</p><p><strong>Prepare 阶段</strong></p><p>TM 向所有 RM 发送 prepare 指令，RM 接受到指令后，执行数据修改和日志记录等操作，然后返回可以提交或者不提交的消息给 TM。</p><p>如果事务协调者 TM 收到所有参与者都准备好的消息，会通知所有的事务提交，然后进入第二阶段。</p><p><strong>Commit 阶段</strong></p><p>TM 接受到所有 RM 的 prepare 结果，如果有 RM 返回是不可提交或者超时，那么向所有 RM 发送 Rollback 命令。</p><p>如果所有 RM 都返回可以提交，那么向所有 RM 发送 Commit 命令，完成一次事务操作。</p><p><strong>MySQL 如何实现 XA 规范</strong></p><p>MySQL 中 XA 事务有两种情况，内部 XA 和外部 XA，其区别是事务发生在 MySQL 服务器单机上，还是发生在多个外部节点间上。</p><p><strong>内部 XA</strong></p><p>在 MySQL 的 InnoDB 存储引擎中，开启 binlog 的情况下，MySQL 会同时维护 binlog 日志与 InnoDB 的 redo log，为了保证这两个日志的一致性，MySQL 使用了 XA 事务，由于是在 MySQL 单机上工作，所以被称为内部 XA。</p><p>内部 XA 事务由 binlog 作为协调者，在事务提交时，则需要将提交信息写入二进制日志，也就是说，binlog 的参与者是 MySQL 本身。</p><p><strong>外部 XA</strong></p><p>外部 XA 就是典型的分布式事务，MySQL 支持 XA START/END/PREPARE/Commit 这些 SQL 语句，通过使用这些命令，可以完成分布式事务。</p><p>你也可以查看 MySQL 官方文档，了解更多的 XA 命令。</p><p>MySQL 外部 XA 主要应用在数据库代理层，实现对 MySQL 数据库的分布式事务支持，例如开源的数据库中间层，比如淘宝的 TDDL、阿里巴巴 B2B 的 Cobar 等。</p><p>外部 XA 一般是针对跨多 MySQL 实例的分布式事务，需要应用层作为协调者，比如我们在写业务代码，在代码中决定提交还是回滚，并且在崩溃时进行恢复。</p><p><strong>binlog 中的 Xid</strong></p><p>当事务提交时，在 binlog 依赖的内部 XA 中，额外添加了 Xid 结构，binlog 有多种数据类型：</p><ol><li>statement 格式，记录为基本语句，包含 Commit</li><li>row 格式，记录为基于行</li><li>mixed 格式，日志记录使用混合格式</li></ol><p>不论是 statement 还是 row 格式，binlog 都会添加一个 XID_EVENT 作为事务的结束，该事件记录了事务的 ID 也就是 Xid，在 MySQL 进行崩溃恢复时根据 binlog 中提交的情况来决定如何恢复。</p><p><strong>binlog 同步过程</strong></p><p>下面来看看 binlog 下的事务提交过程，整体过程是先写 redo log，再写 binlog，并以 binlog 写成功为事务提交成功的标志。</p><p>​ <img src="https://note.youdao.com/yws/public/resource/4248ff5f4022101b536d851e3e06638c/xmlnote/79F2FA9D6A4E4D248F63303639732111/30476" alt="0"></p><p>当有事务提交时：</p><ul><li>InnoDB 进入 Prepare 阶段，并且 write/sync redo log，写 redo log，将事务的 xid 写入到 redo 日志中，binlog 不作任何操作</li><li>进行 write/sync binlog，写 binlog 日志，也会把 xid 写入到 binlog</li><li>调用 InnoDB 引擎的 commit 完成事务的提交，将 commit 信息写入到 redo 日志中</li></ul><p>如果是在第一步和第二步失败，则整个事务回滚</p><p>如果是在第三步失败，则 MySQL 在重启后会检查 xid 是否已经提交，若没有提交，也就是事务需要重新执行，就会在存储引擎中再执行一次提交操作，保障 redo log 和 binlog 数据的一致性，防止数据丢失。</p><p>实际执行还牵扯到操作系统缓存 buffer 何时同步到文件系统中，所以 MySQL 支持用户自定义在 commit 时如何将 log buffer 中的日志刷到 log file 中，通过变量 innodb_flush_log_at_trx_Commit 的值来决定。</p><p>两阶段提交 (Two-Phase Commit, 简称2PC) ,是为了使基于分布式系统架构下的所有节点在进行事务提交时保持一致性而设计的一种算法。分布式事务通常采用2PC，二阶段提交的算法思路可以概括为： 参与者将操作成败通知协调者，再由协调者根据所有参与者的反馈情报决定各参与者是否要提交操作还是中止操作，这里的参与者可以理解为RM，协调者可以理解为TM。</p><p>XA START xid: 开启一个事务，并将事务置于ACTIVE状态，此后执行的SQL语句都将置于该是事务中。</p><p>XA END xid: 将事务置于IDLE状态，表示事务内的SQL操作完成。</p><p>XA PREPARE xid: 实现事务提交的准备工作，事务状态置于PREPARED状态。事务如果无法完成提交前的准备操作，该语句会执行失败。</p><p>XA COMMIT xid: 事务最终提交，完成持久化。</p><p>XA ROLLBACK xid: 事务回滚终止。</p><p>XA RECOVER: 查看MySQL中存在的PREPARED状态的xa事务。</p><p>​ <img src="https://note.youdao.com/yws/public/resource/4248ff5f4022101b536d851e3e06638c/xmlnote/B4BA902BD5E0406ABD928C8508532218/30486" alt="0"></p><p>上面介绍了MySQL XA 的原理，我们现在举几个简单的例子。</p><p>例子1，两阶段XA事务提交：</p><p>mysql&gt; xa start &#39;mysql57xa&#39;;</p><p>Query OK, 0 rows affected (0.00 sec)</p><p>mysql&gt; insert into t1(id) values(1);</p><p>Query OK, 1 row affected (0.00 sec)</p><p>mysql&gt; xa end &#39;mysql57xa&#39;;</p><p>Query OK, 0 rows affected (0.00 sec)</p><p>mysql&gt; xa prepare &#39;mysql57xa&#39;;</p><p>Query OK, 0 rows affected (0.00 sec)</p><p>mysql&gt; xa recover\\G</p><p>formatID: 1</p><p>gtrid_length: 7</p><p>bqual_length: 0</p><p>data: mysql57</p><p>1 row in set (0.00 sec)</p><p>mysql&gt; xa commit &#39;mysql57xa&#39;;</p><p>Query OK, 0 rows affected (0.00 sec)</p><p>例子2，xa commit one phase ，不需要等待其他RM反馈prepare的结果。</p><p>mysql&gt; xa start &#39;mysql57xa&#39;;</p><p>Query OK, 0 rows affected (0.00 sec)</p><p>mysql&gt; insert into t1(id) values(2);</p><p>Query OK, 1 row affected (0.00 sec)</p><p>mysql&gt; xa end &#39;mysql57xa&#39;;</p><p>Query OK, 0 rows affected (0.00 sec)</p><p>mysql&gt; xa commit &#39;mysql57xa&#39; one phase;</p><p>Query OK, 0 rows affected (0.00 sec)</p><p>例子3，在xa prepare后，执行xa rollback 回滚事务。</p><p>mysql&gt; xa start &#39;mysql57xa&#39;;</p><p>Query OK, 0 rows affected (0.00 sec)</p><p>mysql&gt; insert into t1(id) values(3);</p><p>Query OK, 1 row affected (0.00 sec)</p><p>mysql&gt; xa end &#39;mysql57xa&#39;;</p><p>Query OK, 0 rows affected (0.00 sec)</p><p>mysql&gt; xa prepare &#39;mysql57xa&#39;;</p><p>Query OK, 0 rows affected (0.00 sec)</p><p>mysql&gt; xa rollback &#39;mysql57xa&#39;;</p><p>Query OK, 0 rows affected (0.00 sec)</p><p>MySQL XA 的限制</p><p>在MySQL 5.7.7 之前，MySQL一直存在一个&quot;bug&quot;。在事务达到PREPARED状态后，客户端断开与MySQL的连接，MySQL 会自动回滚该事务，这个行为不符合分布式事务的规范，MySQL将PREPARED的事务丢失了。之所以MySQL这么实现是因为MySQL 5.7.7 之前PREPARED的事务并不会记录到binlog中。客户端退出后会丢失该信息，如果允许再提交，那么binlog缺少事务信息，会造成主从不一致。</p><p>在MySQL 5.7.7 之后，MySQL 新增了一个XA_prepare_log_event的事件，会把xa start到xa prepare中间的操作记录到Binlog中。Slave读取Relay log 进行回放，当SQL Thread读取到PREPARED的事务后，在读取xa commit或者xa rollback前，会进行一个类似客户端断开的操作，继续读取后续的事务信息，不会阻塞SQL Thread的执行。从以上的结果看，Oracle在MySQL 5.7.7 上确实完美的解决了MySQL XA一直存在的一个&quot;bug&quot;。</p><p>MySQL XA 的实践</p><p>本人曾在某公司的分布式数据库项目组中实践过基于MySQL XA的分布式事务。MySQL XA 要满足线上高并发的访问要求，在使用时还需要解决两个问题：分布式死锁问题和分布式读一致性问题。分布式死锁问题是指MySQL Server 是可以检测和解决单个MySQL实例中的死锁问题，但涉及到跨越多个MySQL 实例的分布式事务时候，需要程序层面实现死锁的检测和解决。分布式读一致性问题是指MySQL的read view 也是实例级别的，对于全局分布式事务来说无法实现读一致，只能通过select ... lock in share mode在读请求上加锁的串行化隔离级别来实现，这必然会带来并发性能的下降。这就需要在程序层面构建全局的read view来实现全局的MVCC 。当然这两个问题，当时团队的大牛们都已经解决了。</p>',89),a=[s];function l(n,i,y,c,m,g){return e(),o("div",null,a)}const A=p(r,[["render",l]]);export{M as __pageData,A as default};
