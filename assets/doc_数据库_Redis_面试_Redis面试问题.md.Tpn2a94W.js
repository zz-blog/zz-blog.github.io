import{_ as i,c as r,o as l,U as a,m as e}from"./chunks/framework.9QHaLlVJ.js";const f=JSON.parse('{"title":"Redis面试题","description":"","frontmatter":{},"headers":[],"relativePath":"doc/数据库/Redis/面试/Redis面试问题.md","filePath":"packages/doc/src/数据库/Redis/面试/Redis面试问题.md","lastUpdated":1707061484000}'),s={name:"doc/数据库/Redis/面试/Redis面试问题.md"},t=a('<h1 id="redis面试题" tabindex="-1">Redis面试题 <a class="header-anchor" href="#redis面试题" aria-label="Permalink to &quot;Redis面试题&quot;">​</a></h1><h2 id="_1-ziplist压缩链表的实现" tabindex="-1">1.ziplist压缩链表的实现 <a class="header-anchor" href="#_1-ziplist压缩链表的实现" aria-label="Permalink to &quot;1.ziplist压缩链表的实现&quot;">​</a></h2><h2 id="_2-skiplist跳表的实现" tabindex="-1">2.skiplist跳表的实现 <a class="header-anchor" href="#_2-skiplist跳表的实现" aria-label="Permalink to &quot;2.skiplist跳表的实现&quot;">​</a></h2><h2 id="_3-大key的解决方案" tabindex="-1">3.大Key的解决方案 <a class="header-anchor" href="#_3-大key的解决方案" aria-label="Permalink to &quot;3.大Key的解决方案&quot;">​</a></h2><h2 id="_4-热点key解决方案" tabindex="-1">4.热点Key解决方案 <a class="header-anchor" href="#_4-热点key解决方案" aria-label="Permalink to &quot;4.热点Key解决方案&quot;">​</a></h2><h4 id="_1-如何解决高并发热点key" tabindex="-1">1.如何解决高并发热点key？ <a class="header-anchor" href="#_1-如何解决高并发热点key" aria-label="Permalink to &quot;1.如何解决高并发热点key？&quot;">​</a></h4><p>​ 1.数据分片</p><p>​ 2.本地缓存</p><p>​ 3.读写分离</p><h4 id="_2-如何发现是热点key" tabindex="-1">2.如何发现是热点key？ <a class="header-anchor" href="#_2-如何发现是热点key" aria-label="Permalink to &quot;2.如何发现是热点key？&quot;">​</a></h4><p>​ 1.客户端集成，改写代码</p><p>​ 2.流式计算系统监控，写入到zookeeper或者etcd这种的，业务系统监控到了，缓存到本地</p><h2 id="_5-scan重复数据扫描" tabindex="-1">5.scan重复数据扫描 <a class="header-anchor" href="#_5-scan重复数据扫描" aria-label="Permalink to &quot;5.scan重复数据扫描&quot;">​</a></h2><h2 id="_6-基于zset实现的延时队列" tabindex="-1">6.基于zset实现的延时队列 <a class="header-anchor" href="#_6-基于zset实现的延时队列" aria-label="Permalink to &quot;6.基于zset实现的延时队列&quot;">​</a></h2><h2 id="_7-布隆过滤器" tabindex="-1">7.布隆过滤器 <a class="header-anchor" href="#_7-布隆过滤器" aria-label="Permalink to &quot;7.布隆过滤器&quot;">​</a></h2><h2 id="_8-分布式锁" tabindex="-1">8.分布式锁 <a class="header-anchor" href="#_8-分布式锁" aria-label="Permalink to &quot;8.分布式锁&quot;">​</a></h2><h2 id="_10-数据分片" tabindex="-1">10.数据分片 <a class="header-anchor" href="#_10-数据分片" aria-label="Permalink to &quot;10.数据分片&quot;">​</a></h2><h2 id="_11-redis-cluster" tabindex="-1">11.redis cluster <a class="header-anchor" href="#_11-redis-cluster" aria-label="Permalink to &quot;11.redis cluster&quot;">​</a></h2><p><strong>Redis 集群没有使用一致性hash, 而是引入了哈希槽的概念</strong></p><p>Redis Cluster 采用虚拟哈希槽分区，所有的键根据哈希函数映射到 0 ~ 16383 整数槽内**，每个key通过CRC16校验后对16384取模来决定放置哪个槽(Slot)，**每一个节点负责维护一部分槽以及槽所映射的键值数据。</p><p>计算公式：slot = CRC16(key) &amp; 16383。</p><ol><li>当需要增加节点时，只需要把其他节点的某些哈希槽挪到新节点就可以了；</li><li>当需要移除节点时，只需要把移除节点上的哈希槽挪到其他节点就行了。</li></ol><h4 id="问题1-为什么是16383" tabindex="-1">问题1：为什么是16383 <a class="header-anchor" href="#问题1-为什么是16383" aria-label="Permalink to &quot;问题1：为什么是16383&quot;">​</a></h4><p><strong>1.如果槽位为65536，发送心跳信息的消息头达8k，发送的心跳包过于庞大。</strong></p><p>如上所述，在消息头中，最占空间的是 <code>myslots[CLUSTER_SLOTS/8]</code>。 当槽位为65536时，这块的大小是: <code>65536÷8÷1024=8kb</code>因为每秒钟，redis节点需要发送一定数量的ping消息作为心跳包，如果槽位为65536，这个ping消息的消息头太大了，浪费带宽。</p><p><strong>2.redis的集群主节点数量基本不可能超过1000个。</strong></p><p>如上所述，集群节点越多，心跳包的消息体内携带的数据越多。如果节点过1000个，也会导致网络拥堵。因此redis作者，不建议redis cluster节点数量超过1000个。 那么，对于节点数在1000以内的redis cluster集群，16384个槽位够用了。没有必要拓展到65536个。</p><p><strong>3.槽位越小，节点少的情况下，压缩率高</strong></p><p>Redis主节点的配置信息中，它所负责的哈希槽是通过一张bitmap的形式来保存的，在传输过程中，会对bitmap进行压缩，但是如果bitmap的填充率slots / N很高的话(N表示节点数)，bitmap的压缩率就很低。 如果节点数很少，而哈希槽数量很多的话，bitmap的压缩率就很低。</p><p>而16384÷8÷1024=2kb，怎么样，神奇不！</p><p>综上所述，作者决定取16384个槽，不多不少，刚刚好！</p><h2 id="_12-gossip协议-为什么不能做主从" tabindex="-1">12.Gossip协议，为什么不能做主从？ <a class="header-anchor" href="#_12-gossip协议-为什么不能做主从" aria-label="Permalink to &quot;12.Gossip协议，为什么不能做主从？&quot;">​</a></h2><p>Gossip protocol 也叫 Epidemic Protocol （流行病协议），实际上它还有很多别名，比如：“流言算法”、“疫情传播算法”等。</p><p>执行过程：</p><p>Gossip 过程是由种子节点发起，当一个种子节点有状态需要更新到网络中的其他节点时，它会随机的选择周围几个节点散播消息，收到消息的节点也会重复该过程，直至最终网络中所有的节点都收到了消息。这个过程可能需要一定的时间，由于不能保证某个时刻所有节点都收到消息，但是理论上最终所有节点都会收到消息，因此它是一个最终一致性协议。</p><ul><li>直接邮件法：直接发送更新数据，当数据发送失败时，将数据缓存下来，然后重传。</li><li>反熵指的是集群中的节点，每隔段时间就随机选择某个其他节点，然后通过互相交换自己的所有数据来消除两者之间的差异，实现数据的最终一致性。<strong>在实现反熵的时候，主要有推、拉和推拉三种方式。</strong></li><li>谣言传播，广泛地散播谣言，它指的是当一个节点有了新数据后，这个节点变成活跃状态，并周期性地联系其他节点向其发送新数据，直到所有的节点都存储了该新数据</li></ul><p><strong>Gossip 的特点（优势）</strong></p><p><strong>1）扩展性</strong></p><p>网络可以允许节点的任意增加和减少，新增加的节点的状态最终会与其他节点一致。</p><p><strong>2）容错</strong></p><p>网络中任何节点的宕机和重启都不会影响 Gossip 消息的传播，Gossip 协议具有天然的分布式系统容错特性。</p><p><strong>3）去中心化</strong></p><p>Gossip 协议不要求任何中心节点，所有节点都可以是对等的，任何一个节点无需知道整个网络状况，只要网络是连通的，任意一个节点就可以把消息散播到全网。</p><p><strong>4）一致性收敛</strong></p><p>Gossip 协议中的消息会以一传十、十传百一样的指数级速度在网络中快速传播，因此系统状态的不一致可以在很快的时间内收敛到一致。消息传播速度达到了 logN。</p><p><strong>5）简单</strong></p><p>Gossip 协议的过程极其简单，实现起来几乎没有太多复杂性。</p><p><strong>Gossip 的缺陷</strong></p><p>分布式网络中，没有一种完美的解决方案，Gossip 协议跟其他协议一样，也有一些不可避免的缺陷，主要是两个：</p><p><strong>1）消息的延迟</strong></p><p>由于 Gossip 协议中，节点只会随机向少数几个节点发送消息，消息最终是通过多个轮次的散播而到达全网的，因此使用 Gossip 协议会造成不可避免的消息延迟。不适合用在对实时性要求较高的场景下。</p><p><strong>2）消息冗余</strong></p><p>Gossip 协议规定，节点会定期随机选择周围节点发送消息，而收到消息的节点也会重复该步骤，因此就不可避免的存在消息重复发送给同一节点的情况，造成了消息的冗余，同时也增加了收到消息的节点的处理压力。而且，由于是定期发送，因此，即使收到了消息的节点还会反复收到重复消息，加重了消息的冗余。</p><h2 id="_13-redis整体架构" tabindex="-1">13.redis整体架构 <a class="header-anchor" href="#_13-redis整体架构" aria-label="Permalink to &quot;13.redis整体架构&quot;">​</a></h2><p>1.单机</p><p>2.主从</p><p>3.主从+sentinal监控</p><p>4.去中心化cluster</p><p>5.主从+proxy分片</p><h2 id="_14-redis-sentinal" tabindex="-1">14.redis sentinal <a class="header-anchor" href="#_14-redis-sentinal" aria-label="Permalink to &quot;14.redis sentinal&quot;">​</a></h2><p>目的：</p><p>​ 1.故障转移，主从自动切换</p><p>​ 2.监控master,slave状态</p><p>​ 3.消息通知</p><p>4.配置中心</p><p><a href="https://blog.csdn.net/wwj17647590781/article/details/119578510" target="_blank" rel="noreferrer">https://blog.csdn.net/wwj17647590781/article/details/119578510</a></p><h2 id="_15-redis主从复制原理过程" tabindex="-1">15.redis主从复制原理过程 <a class="header-anchor" href="#_15-redis主从复制原理过程" aria-label="Permalink to &quot;15.redis主从复制原理过程&quot;">​</a></h2><p><a href="https://www.cnblogs.com/fkaka/p/13029853.html" target="_blank" rel="noreferrer">https://www.cnblogs.com/fkaka/p/13029853.html</a></p><ul><li>建立连接过程：这个过程就是slave跟master连接的过程</li><li>数据同步过程：是master给slave同步数据的过程</li><li>命令传播过程：是反复同步数据</li></ul><h4 id="_1-建立连接" tabindex="-1">1.建立连接 <a class="header-anchor" href="#_1-建立连接" aria-label="Permalink to &quot;1.建立连接&quot;">​</a></h4><ol><li>设置master的地址和端口，保存master的信息（slave of ip port）</li><li>建立socket连接（这个连接做的事情下文会说）</li><li>持续发送ping命令</li><li>身份验证(auth password)</li><li>发送slave端口信息</li></ol><p>在建立连接的过程中，从节点会保存master的地址和端口、主节点master保存从节点slave的端口。</p><h4 id="_2-数据同步阶段过程" tabindex="-1">2.数据同步阶段过程 <a class="header-anchor" href="#_2-数据同步阶段过程" aria-label="Permalink to &quot;2.数据同步阶段过程&quot;">​</a></h4><p>1.全量复制，当从节点第一次连接主节点时，先会执行一次全量复制这次的全量复制是无法避免的</p><p>2.主节点就会发送复制积压缓冲区的数据，然后从节点就会执行bgrewriteaof恢复数据，这也就是部分复制</p><h4 id="_3-命令传播阶段" tabindex="-1">3.命令传播阶段 <a class="header-anchor" href="#_3-命令传播阶段" aria-label="Permalink to &quot;3.命令传播阶段&quot;">​</a></h4><p>当master数据库被修改后，主从服务器的数据不一致后，此时就会让主从数据同步到一致，这个过程称之为命令传播。</p><p>master会将接收到的数据变更命令发送给slave，slave接收命令后执行命令，让主从数据达到一致。</p><p><strong>命令传播阶段的部分复制</strong></p><ul><li>在命令传播阶段出现断网的情况，或者网络抖动时会导致连接断开（connection lost）</li><li>这个时候主节点master还是会继续往replbackbuffer（复制缓冲积压区）写数据</li><li>从节点会继续尝试连接主机（connect to master）</li><li>当从节点把自己的runid和复制偏移量发送给主节点，并且执行pysnc命令同步</li><li>如果master判断偏移量是在复制缓冲区范围内，就会返回continue命令。并且发送复制缓冲区的数据给从节点。</li><li>从节点接收数据执行bgrewriteaof，恢复数据</li></ul><h4 id="_4-心跳机制" tabindex="-1">4.心跳机制 <a class="header-anchor" href="#_4-心跳机制" aria-label="Permalink to &quot;4.心跳机制&quot;">​</a></h4><p>节点在主从复制的期间是一直在接收客户端的数据，主节点的offset是一直变化的。只有有变化就会给每个slave进行发送，这个发送的过程称之为心跳机制</p><p>在命令传播阶段是，主节点与从节点之间一直都需要进行信息互换，使用心跳机制进行维护，实现主节点和从节点连接保持在线。</p>',83),p=e("ul",null,[e("li",null,[e("p",null,"master心跳")]),e("li",null,[e("ul",null,[e("li",null,"指令：ping"),e("li",null,"默认10秒进行一次，是由参数repl-ping-slave-period决定的"),e("li",null,"主要做的事情就是判断从节点是否在线"),e("li",null,"可以使用info replication 来查看从节点租后一次连接时间的间隔，lag为0或者为1就是正常状态。")])]),e("li",null,[e("p",null,"slave心跳任务")]),e("li",null,[e("ul",null,[e("li",{offset:""},"指令：replconf ack"),e("li",null,"每秒执行一次"),e("li",null,"主要做的事情是给主节点发送自己的复制偏移量，从主节点获取到最新的数据变更命令，还做一件事情就是判断主节点是否在线。")])])],-1),o=a('<p><strong>心跳阶段的注意事项</strong></p><p>主节点为保障数据稳定性，当从节点挂掉的数量或者延迟过高时。将会拒绝所有信息同步。</p><p>这里有俩个参数可以进行配置调整：</p><p>min-slaves-to-write 2</p><p>min-slaves-max-lag 8</p><p>这俩个参数表示从节点的数量就剩余2个，或者从节点的延迟大于8秒时，主节点就会强制关闭maste功能，停止数据同步。</p><p>那么主节点是如何知道从节点挂掉的数量和延迟时间呢！ 在心跳机制里边slave 会每隔一秒发送perlconf ack 这个指令，这个指令可携带偏移量，也可以携带从节点的延迟时间和从节点的数量。</p><h4 id="_5-部分复制的三个核心要素" tabindex="-1">5.部分复制的三个核心要素 <a class="header-anchor" href="#_5-部分复制的三个核心要素" aria-label="Permalink to &quot;5.部分复制的三个核心要素&quot;">​</a></h4><h2 id="_1-服务器的运行id-run-id" tabindex="-1">1. 服务器的运行id （run id） <a class="header-anchor" href="#_1-服务器的运行id-run-id" aria-label="Permalink to &quot;1. 服务器的运行id （run id）&quot;">​</a></h2><p>redis在启动时会自动生成一个随机的id（这里需要注意的是每次启动的id都会不一样），是由40个随机的十六进制字符串组成，用来唯一识别一个redis节点。</p><p>在主从复制初次启动时，master会把自己的runid发送给slave，slave会保存master的这个id，我们可以使用info命令查看</p><p>当断线重连时，slave把这个id发送给master，如果slave保存的runid与master现在的runid相同，master会尝试使用部分复制（这块能否复制成功还有一个因素就是偏移量）。如果slave保存的runid与master现在的runid不同，则会直接进行全量复制。</p><h4 id="_2-复制积压缓冲区" tabindex="-1">2.复制积压缓冲区 <a class="header-anchor" href="#_2-复制积压缓冲区" aria-label="Permalink to &quot;2.复制积压缓冲区&quot;">​</a></h4><p>复制缓冲积压区是一个先进先出的队列，用户存储master收集数据的命令记录。复制缓冲区的默认存储空间是1M。</p><p>可以在配置文件修改<code>repl-backlog-size 1mb</code>来控制缓冲区大小，这个比例可以根据自己的服务器内存来修改，咔咔这边是预留出了30%左右。</p><p>那么复制积压缓冲区就是存储的aof持久化的数据，并且以字节分开，并且每个字节都有自己的偏移量。这个偏移量也就是复制偏移量（offset）</p><p><strong>那为什么会说复制缓冲积压区有可能会导致全量复制呢</strong></p><p>在命令传播阶段，主节点会把收集的数据存储到复制缓冲区中，然后在发送给从节点。就是这里出现了问题，当主节点数据量在一瞬间特别大的时候，超出了复制缓冲区的内存，就会有一部分数据会被挤出去，从而导致主节点和从节点的数据不一致。从而进行全量复制。如果这个缓冲区大小设置不合理那么很大可能会造成死循环，从节点就会一直全量复制，清空数据，全量复制。</p><h4 id="_3-复制偏移量-offset" tabindex="-1">3.复制偏移量（offset） <a class="header-anchor" href="#_3-复制偏移量-offset" aria-label="Permalink to &quot;3.复制偏移量（offset）&quot;">​</a></h4><p>主节点复制偏移量是给从节点发送一次记录一次，从节点是接收一次记录一次。</p><p>用于同步信息，对比主节点和从节点的差异，当slave断联时恢复数据使用。</p><p>这个值也就是来自己于复制缓冲积压区里边的那个偏移量。</p><h4 id="问题1-主节点重启问题-内部优化" tabindex="-1">问题1：主节点重启问题（内部优化） <a class="header-anchor" href="#问题1-主节点重启问题-内部优化" aria-label="Permalink to &quot;问题1：主节点重启问题（内部优化）&quot;">​</a></h4><p>当主节点重启后，runid的值将发生变化，会导致所有的从节点进行全量复制。</p><p>这个问题我们无需考虑，知道系统是怎么优化的即可。</p><p>在建立完主从复制后主节点会创建master-replid变量，这个生成的策略跟runid一样，长度是41位，runid长度是40位，然后发送给从节点。</p><p>在主节点执行shutdown save命令时，进行了一次RDB持久化会把runid 和 offset保存到RDB文件中。可以使用命令redis-check-rdb查看该信息。</p><p>主节点重启后加载RDB文件，将文件中的repl-id 和repl-offset加载到内存中。纵使让所有从节点认为还是之前的主节点。</p><h4 id="问题2-从节点网络中断偏移量越界导致全量复制" tabindex="-1">问题2：从节点网络中断偏移量越界导致全量复制 <a class="header-anchor" href="#问题2-从节点网络中断偏移量越界导致全量复制" aria-label="Permalink to &quot;问题2：从节点网络中断偏移量越界导致全量复制&quot;">​</a></h4><p>由于网络环境不佳，从节点网络中断。复制积压缓冲区内存过小导致数据溢出，伴随着从节点偏移量越界，导致全量复制。有可能会导致反复的全量复制。</p><p>解决方案：修改复制积压缓冲区的大小：repl-backlog-size</p><h4 id="问题3-频繁的网路中断" tabindex="-1">问题3：频繁的网路中断 <a class="header-anchor" href="#问题3-频繁的网路中断" aria-label="Permalink to &quot;问题3：频繁的网路中断&quot;">​</a></h4><p>由于主节点的cpu占用过高，或者从节点频繁连接。出现这种情况造成的结果就是主节点各种资源被严重占用，其中包括但不限于缓冲区，宽带，连接等。</p><p>为什么会出现主节点资源被严重占用？</p><p>在心跳机制中，从节点每秒会发送一个指令replconf ack指令到主节点。</p><p>从节点执行了慢查询，占用大量的cpu</p><p>主节点每秒调用复制定时函数replicationCron，然后从节点长时间没有相应。</p><p>设置从节点超时释放</p><p>设置参数：repl-timeout</p><p>这个参数默认为60秒。超过60秒，释放slave。</p><h4 id="问题4-数据不一致问题" tabindex="-1">问题4：数据不一致问题 <a class="header-anchor" href="#问题4-数据不一致问题" aria-label="Permalink to &quot;问题4：数据不一致问题&quot;">​</a></h4><p>由于网络因素，多个从节点的数据会不一致。这个因素是没有办法避免的。</p><p>关于这个问题给出俩个解决方案：</p><p>第一个数据需要高度一致配置一台redis服务器，读写都用一台服务器，这种方式仅限于少量数据，并且数据需高度一直。</p><p>第二个监控主从节点的偏移量，如果从节点的延迟过大，暂时屏蔽客户端对该从节点的访问。设置参数为slave-serve-stale-data yes|no。 这个参数一但设置就只能响应info slaveof等少数命令。</p><h2 id="_16-redis的pipeline" tabindex="-1">16.redis的pipeline <a class="header-anchor" href="#_16-redis的pipeline" aria-label="Permalink to &quot;16.redis的pipeline&quot;">​</a></h2><p>Redis 管道 (Pipeline) 本身并不是 Redis 服务器直接提供的技术， 这个技术本质上是由客户端提供的，</p><p>edis客户端执行一条命令分4个过程：发送命令－〉命令排队－〉命令执行－〉返回结果</p><p>这个过程称为Round trip time(简称RTT, 往返时间)，mget mset有效节约了RTT，但大部分命令（如hgetall，并没有mhgetall）不支持批量操作，需要消耗N次RTT ，这个时候需要pipeline来解决这个问题</p><p>1、原生批命令是原子性，pipeline是非原子性 (原子性概念:一个事务是一个不可分割的最小工作单位,要么都成功要么都失败。原子操作是指你的一个业务逻辑必须是不可拆分的. 处理一件事情要么都成功，要么都失败，原子不可拆分)</p><p>2、原生批命令一命令多个key, 但pipeline支持多命令（存在事务），非原子性 3、原生批命令是服务端实现，而pipeline需要服务端与客户端共同完成</p><p>使用pipeline组装的命令个数不能太多，不然数据量过大，增加客户端的等待时间，还可能造成网络阻塞，可以将大量命令的拆分多个小的pipeline命令完成。</p><p>本质：客户端通过改变了读写的顺序提高性能。</p><h2 id="_17-redis如何实现事务" tabindex="-1">17.redis如何实现事务？ <a class="header-anchor" href="#_17-redis如何实现事务" aria-label="Permalink to &quot;17.redis如何实现事务？&quot;">​</a></h2><h2 id="_18-redis缓存击穿-缓存穿透-缓存雪崩" tabindex="-1">18.redis缓存击穿，缓存穿透，缓存雪崩 <a class="header-anchor" href="#_18-redis缓存击穿-缓存穿透-缓存雪崩" aria-label="Permalink to &quot;18.redis缓存击穿，缓存穿透，缓存雪崩&quot;">​</a></h2><h4 id="缓存击穿-key在过期的一瞬间-大量请求到db的场景" tabindex="-1">缓存击穿：key在过期的一瞬间，大量请求到DB的场景 <a class="header-anchor" href="#缓存击穿-key在过期的一瞬间-大量请求到db的场景" aria-label="Permalink to &quot;缓存击穿：key在过期的一瞬间，大量请求到DB的场景&quot;">​</a></h4><p>方案：</p><p>1.请求数据库加互斥锁，然后写入缓存，之后都走缓存，拿不到锁的期间就阻塞等着或者直接返回。</p><p>2.热点数据永不过期，只更新不删除</p><h4 id="缓存雪崩-大量不同的key同时过期" tabindex="-1">缓存雪崩：大量不同的key同时过期 <a class="header-anchor" href="#缓存雪崩-大量不同的key同时过期" aria-label="Permalink to &quot;缓存雪崩：大量不同的key同时过期&quot;">​</a></h4><p>方案：</p><p>1.过期时间业务打撒，过期时间随机化</p><p>2.热点数据不过期</p><p>3.互斥锁</p><h4 id="缓存穿透-大量key不存在-直接到db" tabindex="-1">缓存穿透：大量key不存在，直接到DB <a class="header-anchor" href="#缓存穿透-大量key不存在-直接到db" aria-label="Permalink to &quot;缓存穿透：大量key不存在，直接到DB&quot;">​</a></h4><p>方案</p><p>1.接口校验</p><p>2.缓存空值</p><p>3.布隆过滤器判断key,可能误判，与HASHMAP相比空间只能用少，空间占用与hash函数个数相关</p>',69),n=[t,p,o];function d(h,c,u,_,b,k){return l(),r("div",null,n)}const q=i(s,[["render",d]]);export{f as __pageData,q as default};
