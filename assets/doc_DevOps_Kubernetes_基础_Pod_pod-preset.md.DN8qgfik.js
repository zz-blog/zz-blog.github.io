import{_ as e,c as o,o as t,a4 as d}from"./chunks/framework.CzsDe1xi.js";const m=JSON.parse('{"title":"Pod Preset","description":"","frontmatter":{},"headers":[],"relativePath":"doc/DevOps/Kubernetes/基础/Pod/pod-preset.md","filePath":"packages/doc/src/DevOps/Kubernetes/基础/Pod/pod-preset.md","lastUpdated":1705214817000}'),r={name:"doc/DevOps/Kubernetes/基础/Pod/pod-preset.md"},s=d('<h1 id="pod-preset" tabindex="-1">Pod Preset <a class="header-anchor" href="#pod-preset" aria-label="Permalink to &quot;Pod Preset&quot;">​</a></h1><blockquote><p>**注意：**PodPreset 资源对象只有 Kubernetes 1.8 以上版本才支持。</p></blockquote><p>Preset 就是预设，有时候想要让一批容器在启动的时候就注入一些信息，比如 secret、volume、volume mount 和环境变量，而又不想一个一个的改这些 Pod 的 template，这时候就可以用到 PodPreset 这个资源对象了。</p><p>本页是关于 PodPreset 的概述，该对象用来在 Pod 创建的时候向 Pod 中注入某些特定信息。该信息可以包括 secret、volume、volume mount 和环境变量。</p><h2 id="理解-pod-preset" tabindex="-1">理解 Pod Preset <a class="header-anchor" href="#理解-pod-preset" aria-label="Permalink to &quot;理解 Pod Preset&quot;">​</a></h2><p><code>Pod Preset</code> 是用来在 Pod 被创建的时候向其中注入额外的运行时需求的 API 资源。</p><p>您可以使用 <a href="https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/#label-selectors" target="_blank" rel="noreferrer">label selector</a> 来指定为哪些 Pod 应用 Pod Preset。</p><p>使用 Pod Preset 使得 pod 模板的作者可以不必为每个 Pod 明确提供所有信息。这样一来，pod 模板的作者就不需要知道关于该服务的所有细节。</p><p>关于该背景的更多信息，请参阅 <a href="https://git.k8s.io/community/contributors/design-proposals/service-catalog/pod-preset.md" target="_blank" rel="noreferrer">PodPreset 的设计方案</a>。</p><h2 id="如何工作" tabindex="-1">如何工作 <a class="header-anchor" href="#如何工作" aria-label="Permalink to &quot;如何工作&quot;">​</a></h2><p>Kubernetes 提供了一个准入控制器（<code>PodPreset</code>），当其启用时，Pod Preset 会将应用创建请求传入到该控制器上。当有 Pod 创建请求发生时，系统将执行以下操作：</p><ol><li>检索所有可用的 <code>PodPresets</code>。</li><li>检查 PodPreset 标签选择器上的标签，看看其是否能够匹配正在创建的 Pod 上的标签。</li><li>尝试将由 <code>PodPreset</code> 定义的各种资源合并到正在创建的 Pod 中。</li><li>出现错误时，在该 Pod 上引发记录合并错误的事件，PodPreset <em>不会</em>注入任何资源到创建的 Pod 中。</li><li>注释刚生成的修改过的 Pod spec，以表明它已被 PodPreset 修改过。注释的格式为 <code>podpreset.admission.kubernetes.io/podpreset-&lt;pod-preset name&gt;&quot;: &quot;&lt;resource version&gt;&quot;</code>。</li></ol><p>每个 Pod 可以匹配零个或多个 Pod Prestet；并且每个 <code>PodPreset</code> 可以应用于零个或多个 Pod。 <code>PodPreset</code> 应用于一个或多个 Pod 时，Kubernetes 会修改 Pod Spec。对于 <code>Env</code>、<code>EnvFrom</code> 和 <code>VolumeMounts</code> 的更改，Kubernetes 修改 Pod 中所有容器的容器 spec；对于 <code>Volume</code> 的更改，Kubernetes 修改 Pod Spec。</p><blockquote><p>**注意：**Pod Preset 可以在适当的时候修改 Pod spec 中的 <code>spec.containers</code> 字段。Pod Preset 中的资源定义将<em>不会</em>应用于 <code>initContainers</code> 字段。</p></blockquote><h3 id="禁用特定-pod-的-pod-preset" tabindex="-1">禁用特定 Pod 的 Pod Preset <a class="header-anchor" href="#禁用特定-pod-的-pod-preset" aria-label="Permalink to &quot;禁用特定 Pod 的 Pod Preset&quot;">​</a></h3><p>在某些情况下，您可能不希望 Pod 被任何 Pod Preset 所改变。在这些情况下，您可以在 Pod 的 Pod Spec 中添加注释：<code>podpreset.admission.kubernetes.io/exclude：&quot;true&quot;</code>。</p><h2 id="启用-pod-preset" tabindex="-1">启用 Pod Preset <a class="header-anchor" href="#启用-pod-preset" aria-label="Permalink to &quot;启用 Pod Preset&quot;">​</a></h2><p>为了在群集中使用 Pod Preset，您必须确保以下内容：</p><ol><li>您已启用 <code>settings.k8s.io/v1alpha1/podpreset</code> API 类型。例如，可以通过在 API server 的 <code>--runtime-config</code> 选项中包含 <code>settings.k8s.io/v1alpha1=true</code> 来完成此操作。</li><li>您已启用 <code>PodPreset</code> 准入控制器。 一种方法是将 <code>PodPreset</code> 包含在为 API server 指定的 <code>--admission-control</code> 选项值中。</li><li>您已经在要使用的命名空间中通过创建 <code>PodPreset</code> 对象来定义 <code>PodPreset</code>。</li></ol>',19),P=[s];function c(a,p,l,i,n,u){return t(),o("div",null,P)}const b=e(r,[["render",c]]);export{m as __pageData,b as default};
